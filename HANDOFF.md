# 📦 ส่งงานต่อ — บอทเอด + Dashboard ไส้ย่างซอย๔

> อัปเดต: **2026-08-30** — เอกสารส่งงาน "ฉบับปัจจุบัน" ของทั้งโปรเจกต์ (แทนที่ฉบับ 2026-08-18)
> repo หลัก: **`empro959/line-slip-bot`** branch `main` → Render auto-deploy
> ⚠️ `saiyangsoi-sketch/Line-Slip-Bot` = **repo เก่า เลิกใช้แล้ว** (ห้าม push เพิ่ม — งานทั้งหมดถูกรวมกลับมาที่ empro959 แล้วด้วย merge `bcb7020`)
>
> **เอกสารอื่นในโปรเจกต์:** `ARCHITECTURE.md` (สถาปัตยกรรมบอท + ENV ครบ) · `COORDINATION.md` (กล่องจดหมายระหว่างห้อง — อ่านก่อนเริ่มงานทุกครั้ง) · `dashboard/HANDOFF.md` (รายละเอียดฝั่ง dashboard/POS) · `TEAM.md` (ใครดูแลอะไร)

---

## 1. TL;DR — เหลืออะไรบ้าง (สถานะ 30 ส.ค. 2026)

> ✅ ระบบทั้งหมดทำงานปกติ · Render deploy จาก `empro959/line-slip-bot` (auto ผ่าน GitHub Actions)
> ✅ เทสต์ **220 เคส** เขียวหมด (`python3 -m unittest discover -s tests`)
> ✅ **PR #140 merge แล้ว + deploy สำเร็จ** — merge commit `fd622db` · GitHub Actions run #65 `success`
> (บิลกระดาษไม่นับซ้ำเป็นรายรับ §3.9 · ลบบิลเจาะจงใบได้ §3.13 — **ขึ้น production จริงแล้ว**)
> ⚠️ **ยังไม่เคยตรวจบน production จริง** — พิสูจน์แค่ unit test บนเครื่อง dev (ดู "ระดับการตรวจ" ท้าย §3.9/§3.13)
> ✅ **§3.12 แก้แล้วบน `fix/weekly-expense-payout-range`** (ค่าใช้จ่าย 1 สัปดาห์เคยถูกนับ 7 รอบ)
> **แต่ยังไม่ deploy จริง** — `dashboard/Code_full.gs` ไม่มี clasp/CI ผูกกับ Apps Script เลย
> merge PR ไม่ทำให้โค้ดที่รันจริงเปลี่ยน **ต้องมีคนก๊อปวางมือเข้า Apps Script editor** (ดูขั้นตอนที่ §3.12)
> 📋 **2 ก.ย. — เขียนขั้นตอนคลิกต่อคลิกให้ Cowork/เจ้าของทำจริงแล้ว:** `dashboard/DEPLOY_STEPS_2026-09-02.md`
> (ยังไม่มีใครทำตามขั้นตอนนี้จริง — ห้องนี้แตะ Apps Script editor/Netlify จากในนี้ไม่ได้เลย · แจ้ง Cowork
> ผ่านกล่องกลาง Drive (`ERP-handoff`) แล้วด้วย 2 ก.ย. 23:37 น. ไทย — ไฟล์ `saiyang-to-cowork-02-09-...md`)
> จนกว่าจะ deploy มือเสร็จ **อย่าเชื่อบรรทัด "ค่าใช้จ่าย"/"กำไร" ในสรุปรายวัน/รายสัปดาห์** (ยอดขายใช้ได้ปกติ)
> 🔴 **กติกาเดิมยังใช้เสมอ: ห้าม push `main` ตอนร้านเปิด** — `.github/workflows/render-deploy.yml`
> ไม่มี path filter push อะไรก็ตาม = ยิง Deploy Hook = worker restart ~1 นาที
> ✅ **commit `2398c5b` (เอกสารล้วน — DEPLOY_STEPS + อัปเดต HANDOFF) push แล้ว 02:20 น. ไทย (คืน 2→3 ก.ย.)**
> ตามที่เจ้าของอนุมัติล่วงหน้าไว้แล้วว่า "ร้านตีสอง merge เอง" (คำตอบ 30 ส.ค. ผ่านห้อง Cowork) —
> GitHub Actions run **#68 `success`** · เทสต์ **220/220 เขียว** ก่อน push (รันจริงหลังติดตั้ง
> dependency ใหม่ในคอนเทนเนอร์นี้ — `pip install -r requirements.txt --ignore-installed blinker`)

### 🔴 ค้างที่เจ้าของ — มีกำหนดวัน (เลยกำหนดแล้ว — ยังไม่มีการยืนยัน)
| เมื่อไหร่ | งาน | ที่ไหน |
|---|---|---|
| **31 ส.ค. 11:00** (เลยมาแล้ว) | ลบ Render `DO-NOT-USE-old` (s772) + service `65gt` — **ถ่ายรูปหน้า env ไว้ก่อนลบ** | Render (มี Routine เตือนอัตโนมัติ) |

### ✅ ปิดแล้ว 3 ก.ย. — สลิปจ่ายดวงใจถูก reject ผิด 12,069 บาท (§3.14)
บั๊ก `PAYABLE_PAYEE_KEYWORDS` เป็นชื่อล้วน (ไม่มีเลขบัญชี) ทำให้สลิปจ่ายดวงใจ 2 ใบ (10,329 + 1,740)
คืน 2 ก.ย. ถูกบอทตอบ "ไม่นับลดหนี้" ทั้งที่จ่ายถูกบัญชี — แก้แล้วครบ:
- ✅ แก้ env เพิ่ม `5342` ใน `PAYABLE_PAYEE_KEYWORDS` (ยืนยันด้วยการยิงเข้าฟังก์ชันจริงแล้วว่าผ่าน)
- ✅ เพิ่มคำสั่ง `ลบจ่าย <วัน> <ยอด>` (PR #142, merge แล้ว) ใช้ล้างรายการ `จ่าย 10329` ที่พิมพ์ซ้ำ
  หลังสลิปนับเข้าไปเองทีหลัง (ยอดค้างเคยติดลบเพราะจุดนี้)
- ✅ **เจ้าของยืนยันเอง 3 ก.ย. 00:39 น.: "เรียบร้อยหมดแล้ว"** — ถือว่าทั้งยอดค้าง 12,069 และ
  `PAYABLE_VENDOR` แก้ครบแล้ว (ไม่ได้แจ้งรายละเอียดวิธีแก้ `PAYABLE_VENDOR` มาที่ห้องนี้ — ถ้าเห็น
  ข้อความหนี้ขึ้นช่องว่างอีกให้แจ้งกลับ)
- ดูรายละเอียดต้นเหตุ/ระดับการตรวจเต็มที่ §3.14 ด้านล่าง

> 🔎 **เช็คแล้ว 2 ก.ย. 20:26:** ไม่พบข้อความยืนยันการลบจริงในรีโป (`TEAM.md`/`COORDINATION.md`/`HANDOFF.md`
> เก่า) และห้องนี้เข้า Render dashboard/API ไม่ได้เลย (ไม่มีเครื่องมือ/เครดิตเชื่อมต่อ Render จากในนี้)
> **สถานะ: รอยืนยันจากเจ้าของ** ว่าลบ `s772`/`65gt` ไปแล้วจริงหรือยัง

### 🟠 ค้างที่เจ้าของ — รอตัดสินใจ (คนอื่นรออยู่)
| งาน | รายละเอียด |
|---|---|
| ~~เลือกคืนสลับ webhook (Option B)~~ | ✅ **ทำเสร็จแล้ว 7 ก.ย.** — ดู §3.15 |

### 🟡 ค้างที่เจ้าของ — ต้องใช้ตาคน (ผมช่วยจากรูปไม่ได้)
| งาน | เปิดที่ไหน |
|---|---|
| บิลยกเลิก ฿3,599 (16 ส.ค.) | Easy POS → รายงาน → รายการที่ยกเลิก |
| ส่วนต่าง **7,384** สมุดจด vs ใบเสร็จ (21 ส.ค.) | เบาะแส: รายการดวงใจต่างกัน 3,934 (สมุดลงบิลเก่า 8/8·15/8·19/8 · POS ลง 21/8) เหลืออีก ~3,450 |
| ส่วนต่าง **2,894** สลิป vs โอน (20 ส.ค.) | เลื่อนดูรูปในกลุ่มวันที่ 20 หาสลิปที่บอทไม่ตอบ แล้ว `เพิ่มสลิป <ยอด>` |
| ส่วนต่าง **16,238** เงินรับ vs ขาย (16 ก.ค.) | Easy POS → รายงานรับชำระ/ลูกหนี้ · **ยอดขายยืนยันแล้วว่าครบ ห้ามสั่งอ่านใหม่** |
| **ค่าใช้จ่าย/กำไร ในสรุปเพี้ยน (§3.12)** | ✅ แก้โค้ด + merge เข้า `main` แล้ว (`34468e3`) · ✅ **2 ก.ย.: เขียนขั้นตอนคลิกต่อคลิกแล้ว** — `dashboard/DEPLOY_STEPS_2026-09-02.md` · **รอ Cowork/เจ้าของก๊อปวางมือเข้า Apps Script editor จริง** (merge เข้า `main` เฉยๆ ไม่พอ) |
| **ยอด 30/08 — รายงาน POS ถูกพิมพ์ตอนวันยังไม่จบ (§3.10)** | เทียบยอด 30/08 บน dashboard กับยอดจริง · **อย่าเชื่อเลขที่เด้งเข้าไลน์คืนวันที่ 30** (เป็นเลขของใบ 3 ทุ่มครึ่ง และระบบจะไม่ส่งแก้) · 🔎 **เช็คแล้ว 2 ก.ย. 20:26: ห้องนี้เข้า dashboard จริงไม่ได้** (`inspiring-mooncake-52e91e.netlify.app` โดน network egress proxy บล็อก — ทดสอบยิงตรงแล้วได้ `EGRESS_BLOCKED`) **สถานะ: รอยืนยันจากเจ้าของ/Cowork ที่เปิดเว็บได้จริง** |
| ตรวจจอง **#61** (24 ส.ค.) | `สรุปจอง` — บันทึกไว้ตอนวัน/จำนวนคนยังเพี้ยน ถ้าผิดต้องยกเลิกแล้วจองใหม่ |

### 🔵 รอห้องอื่น (ไม่ใช่เจ้าของ)
- ห้องคอนเทนต์ตอบเรื่อง `line_bridge` (ดู `TEAM.md`)

### ⚪ เสนอไว้ รอเจ้าของสั่ง (ยังไม่เร่ง)
| | งาน | ทำไม |
|---|---|---|
| 1 | เพดานเตือน "ยกเลิกสูง" > ฿3,000 อาจต่ำไป → เปลี่ยนเป็น % ของยอดขาย | เตือนบ่อยจนชิน แล้ววันที่ผิดจริงจะกลืนไป |
| 2 | ตัวแกะรายเมนู POS พลาด (21 ส.ค. หาย 417) | กระทบแค่ Menu Engineering ไม่กระทบเงิน |
| 3 | PWA แดชบอร์ด (เจ้าของสั่งพักไว้ 22 ส.ค.) | ขาด `manifest.webmanifest` + `sw.js` เท่านั้น ฟรี |
| 4 | Menu Engineering | ต้องการต้นทุนเมนู top-20 (`All_menu.xlsx` มีต้นทุนแค่ 51/936 รายการ) |

### ✅ ปิดจบแล้ว (ไม่ต้องหยิบมาถามใหม่)
- ย้าย Render มา `empro959` + auto-deploy ผ่าน GitHub Actions (19 ส.ค.)
- กู้ยอดขาย POS ที่ผิด **~172,000 บาท** ใน 5 วัน (19 ส.ค. · ดู `dashboard/HANDOFF.md` §6.1)
- ยอดสลิปไม่ตรง — บั๊ก 11 ตัว กู้คืน **6,446 บาท** (20 ส.ค. · §3.7)
- **บั๊กชุด 22–24 ส.ค. — 13 ตัว (§3.8)**
- **ใบแจ้งรายการนับซ้ำเป็นรายรับ (§3.9) — merge + deploy แล้ว 2 ก.ย.** (`fd622db`)
  ⚠️ ยังไม่เคยลองกับรูปบิลจริงผ่าน Gemini บน production — ถ่ายบิลจริงยืนยันสักครั้งเมื่อมีจังหวะ
- **ลบบิลผิดใบ/คำสั่งไม่รู้จักเงียบสนิท (§3.13) — merge + deploy แล้ว 2 ก.ย.** (`fd622db`)
  ⚠️ ยังไม่เคยสั่งคำสั่ง `ลบบิล <วันที่> <ยอด>` จริงบน production — ลองสั่งจริงยืนยันสักครั้งเมื่อมีจังหวะ
- **ใบแจ้งรายการถูกนับเป็นรายรับซ้ำ (30 ส.ค. · §3.9)** — 3 ใน 4 กลุ่มรับสลิปไม่เคยมีด่านจับบิลเลย
- 25 ก.ค. / 29 ก.ค. ไม่ต้องกู้ · ปลายทางรายงานสลิปไม่ย้าย · แยก/รวมบัญชีจบแล้ว (§3.5)

---

## 2. ระบบทั้งหมดต่อกันอย่างไร (สำคัญ — ใช้เวลาไล่ทั้งวันจึงได้แผนนี้)

```
┌─ ระบบ A: บอทเอด (LINE) ─────────────────────────────────────────┐
│  app.py (Flask, ไฟล์เดียว) → Render "Line-Slip-Bot-1"           │
│  DB: Supabase Postgres (Session pooler, IPv4)                    │
│  · ตรวจสลิปรายรับ (Gemini) + รายงานสรุป 00:30                    │
│  · จองโต๊ะ                                                       │
│  · บัญชีหนี้เจ้าหนี้ (ดวงใจการสุรา) — เด้งสรุปทุกครั้งที่มีบิล/จ่าย │
│  ตัวปลุกงานตามเวลา = ping /health (UptimeRobot) + thread สำรอง    │
│  ⚠️ ช่วง "เงียบลึก" 03:00–10:00 หยุดงานตามเวลาทั้งหมด            │
└──────────────────────────────────────────────────────────────────┘
        ▲ /api/slip_daily (GET)          ▼ /api/push_owner (POST)
        │ (ยอดสลิปรายวัน ให้เทียบ)        │ (ยิงข้อความเข้าไลน์เจ้าของ)
┌───────┴─────────────────────────────────┴────────────────────────┐
│  ระบบ B: Apps Script "Dashboard Saiyang Soi4" (ไฟล์เดียว รหัส.gs) │
│  · POS ส่งเมล PDF ทุกคืน ~00:3x → importPosReports (trigger ตี1–5)│
│    → OCR ด้วย Drive → pos_daily.json + saisang_data.json (Drive)  │
│  · sendDailyAlert → LINE (ผ่านบอท) + อีเมลสรุป                    │
│  · doGet/doPost = ให้ dashboard อ่าน/เขียนข้อมูล                  │
│  Script Properties: SLIP_API_URL (มี token), OWNER_EMAILS         │
└──────────────────────────────────────────────────────────────────┘
                              ▼ doGet
                    Dashboard (Netlify, team saiyangsoi)
```

**จุดที่คนพลาดบ่อย:** "สรุปรายรับ" **ไม่ได้มาจากบอท** — มาจาก Apps Script ส่วน "รายงานสลิป 00:30" มาจากบอท คนละตัวกัน อาการเหมือนกันแต่แก้คนละที่

---

## 3. ทะเบียนบริการทั้งหมด (นับครบ 18 ส.ค. 2026 — 12 ตัวใช้งาน + 1 เตรียมไว้)

### 3.1 ตัวที่ระบบใช้งานอยู่

| # | บริการ | บ้าน (บัญชี) | หน้าที่ | เงิน | สถานะ |
|---|---|---|---|---|---|
| 1 | LINE OA "เอด" | LINE Business | ตัวบอทหลัก — webhook → iq8e `/callback` | ฟรี (push ~300/ด./OA) | ✅ ปกติ |
| 2 | LINE OA สำรอง (OA2..) | LINE Business | สลับช่องเมื่อโควตา push เต็ม (`OA_ROUTE`) | ฟรี | ✅ ตามที่ตั้ง token |
| 3 | Render `Line-Slip-Bot-1` (iq8e) | ร้าน (saiyangsoi@gmail) | รันบอทตัวจริง | **Starter ~$7/ด. — ตัวเดียวที่จ่ายรายเดือน** | ✅ ดึง `empro959/line-slip-bot` แบบ Public Git URL (deploy `b6d7a24` 19 ส.ค.) · auto-deploy ผ่าน GitHub Actions + Deploy Hook |
| 4 | Supabase org `saiyangsoi4` | ร้าน | DB production (Session pooler IPv4) | ฟรี | ⚠️ ยังไม่เปิดยืนยัน `DATABASE_URL` |
| 5 | Gemini API (`project-for-key-3`) | Google Cloud ร้าน | อ่านสลิป/บิลจากรูป | จ่ายตามใช้ (เปิด billing กัน 429) | ✅ ปกติ |
| 6 | Gmail | ร้าน | รับเมลรายงาน PDF จาก POS ทุกคืน ~00:3x | ฟรี | ✅ ปกติ |
| 7 | Apps Script + Drive | ร้าน | OCR ใบ POS → ข้อมูล dashboard + สรุปรายรับเข้าไลน์/เมล | ฟรี (โควตา OCR/วัน) | ✅ วางโค้ดล่าสุดแล้ว |
| 8 | Netlify (team saiyangsoi) | ร้าน | โฮสต์หน้า dashboard | ฟรี | ✅ (deploy มือ ลาก zip) |
| 9 | UptimeRobot | ⚠️ **บัญชี E&M ProEng — ปนกับงานอื่น** | ตัวปลุกรายงาน 00:30 (ping `/health` ทุก 5 นาที) | ฟรี | ✅ เขียว · ควรย้ายมาบัญชีร้านสักวัน |
| 10 | GitHub `empro959/line-slip-bot` | ส่วนตัว (empro959) | **บ้านของโค้ด** — โค้ดล่าสุดทั้งหมด | ฟรี | ✅ Render ชี้มาที่นี่แล้ว (19 ส.ค.) |
| 11 | GitHub `saiyangsoi-sketch/Line-Slip-Bot` | ร้าน — **ล็อกอินหาย** | โค้ดเก่า — ไม่มีอะไรดึงแล้ว | ฟรี | 🪦 เลิกใช้แล้ว 19 ส.ค. (iq8e สลับออกแล้ว — ห้าม push/ดึงอีก) |
| 12 | Easy POS | ผู้ขาย POS (ภายนอก) | ระบบขายหน้าร้าน + ส่งเมลรายงานทุกคืน — ต้นทางข้อมูลทั้งหมดของ dashboard | ตามสัญญา POS | ✅ ปกติ |

### 3.2 ของเก่าที่ปลดระวางแล้ว

| บริการ | สถานะ |
|---|---|
| Render `Line-Slip-Bot` (s772) | Suspended · ต่อ empro959 @`785bbb7` — ลบ/เปลี่ยนชื่อ `DO-NOT-USE` เมื่อย้ายเสร็จ |
| Render `65gt` | Suspended · ⚠️ **ป้ายเดิมเขียนว่า "E&M เก่า" — ผิด** · เป็น **เซอร์วิสเก่าของบอทร้านเอง** (`line-slip-bot-65gt.onrender.com` · `TEAM.md:123`) ป้ายผิดนี้ทำให้กล่องกลางเถียงกันว่าใครเป็นเจ้าของ (30 ส.ค.) |
| Supabase `empro959` | จะโดน pause — **export เก็บครบแล้ว 18 ส.ค.** ✅ |
| Gemini E&M | เลิกใช้ |

### 3.3 เตรียมไว้แต่ยังไม่เปิดใช้

| บริการ | หมายเหตุ |
|---|---|
| SlipOK API (`api.slipok.com`) | ตรวจสลิปแม่น 100% (ตอนนี้ Gemini ~98%) — โค้ดรองรับแล้ว แค่ตั้ง `PROMPTPAY_API_KEY` · มีค่ารายเดือน ชั่งน้ำหนักก่อนเปิด |

### 3.4 เส้นเชื่อม "ใครจำ URL/กุญแจใคร" — ไล่ให้ครบทุกครั้งที่ย้ายอะไร

ทุกครั้งที่ระบบพังเงียบในโปรเจกต์นี้ ต้นเหตุคือเส้นพวกนี้ชี้ไปที่เก่า:

```
LINE OA ── webhook ──→ line_bridge (Apps Script ห้องคอนเทนต์) ──forward──→ iq8e /callback
   ⚠️ ยืนยันด้วยตา 19 ส.ค.: webhook = script.google.com/macros/s/AKfycb24qjDXw…/exec
   ⚠️ = บอทร้านฝากทางเข้าไว้กับสคริปต์ของอีกห้อง · เขา deploy พลาดเมื่อไหร่ร้านดับ
   ทางลด: app.py มี WEBHOOK_FORWARD_URLS (ปิดอยู่) ไว้สลับให้ Render เป็นทางเข้าแทน
UptimeRobot ── ping ────────→ iq8e /health
Apps Script ── SLIP_API_URL ─→ iq8e (มี token ฝังใน property)
บอท ────────── SYNC_URL ────→ Apps Script /exec
บอท ────────── DATABASE_URL → Supabase saiyangsoi4
บอท ────────── GEMINI_API_KEY → Google Cloud ร้าน
Render iq8e ── ดึงโค้ด (Public Git URL) → GitHub empro959/line-slip-bot ✅
GitHub Actions ─ push main ยิง Deploy Hook → Render iq8e (secret RENDER_DEPLOY_HOOK)
```

**กติกา: ย้ายอะไรก็ตาม ให้เปิดตารางนี้แล้วถามว่า "เส้นไหนชี้มาที่ของที่ย้าย" แล้วไล่แก้ + ยืนยันด้วยตาให้ครบก่อนปิดงาน**

### 3.5 ⚖️ ข้อยุติ "แยกบัญชี vs รวมบัญชี" — ✅ เจ้าของเคาะแล้ว 19 ส.ค. 2026

**คำตัดสิน: "อยู่ที่เดิมทุกตัว" — ไม่ย้ายบัญชีอะไรอีกทั้งสิ้น**
(น้ำหนักที่ใช้ตัดสิน: ความสะดวกในการทำงานของเจ้าของ+Claude ล้วนๆ · ตัดเรื่องความลับและค่าใช้จ่ายออก)

**หลัก 3 ข้อที่ใช้แทนการเถียงรวม/แยก:**
1. **ของที่จับบ่อย → เกาะบัญชีที่ล็อกอินประจำอยู่แล้ว** — GitHub เกาะ `empro959` (Claude ผูกอยู่ ทำงานทุก session) · เมลร้านเกาะ `saiyangsoi` (เจ้าของเปิดทุกวัน)
2. **ของที่นานๆ เปิดที → ห้ามมีบัญชีแยกของตัวเอง** — บัญชีที่ไม่ได้ล็อกอินประจำจะเน่าเอง (หลักฐาน: `saiyangsoi-sketch` รหัสหาย)
3. **ศัตรูตัวจริงคือ "ของซ้ำ + ไม่รู้ว่าอะไรอยู่ไหน"** — แก้ด้วยทะเบียนนี้ + ตั้งชื่อระบุกิจการ + ลบของซ้ำ

**📌 ตารางถาวร: บริการไหนอยู่บัญชีอะไร (ท่องไว้ ไม่ต้องเดาอีก)**

| บัญชี (login) | บริการที่อยู่ในนั้น |
|---|---|
| **ร้าน — `saiyangsoi@gmail.com`** | Render (service `Line-Slip-Bot-1`/iq8e = ตัวจริง) · Supabase org `saiyangsoi4` · Gmail · Apps Script · Drive · Google Cloud (คีย์ Gemini `project-for-key-3`) · Netlify (team saiyangsoi) |
| **ส่วนตัว — GitHub `empro959`** | `empro959/line-slip-bot` = **บ้านโค้ดถาวรที่เดียว** (public · สิทธิ์ให้รายห้องได้ · Claude push ที่นี่) |
| **E&M ProEngineering** | UptimeRobot (monitor ของร้านอยู่รวมกับของ E&M — **แยกด้วยชื่อ** `[ร้าน]`/`[E&M]`) |
| **LINE Business (บัญชีเดียว)** | OA "เอด" + OA สำรองทั้งหมด — แยกเป็นห้องรายา OA · โควตา push คิดต่อ OA |
| **ผู้ขายภายนอก** | Easy POS (ส่งเมลรายงานเข้า Gmail ร้าน) |
| **🪦 เลิกใช้ — ห้ามหยิบมาใช้อีก** | GitHub `saiyangsoi-sketch` (รหัสหาย ไม่ต้องกู้) · Render `s772` (ลบ/เปลี่ยนชื่อ DO-NOT-USE) · Render `65gt` · Supabase `empro959` (export แล้ว) · Gemini E&M |

**งานเก็บตกจากข้อยุตินี้ (ไม่มีการย้ายบัญชีเลย):**
1. ✅ ~~ชี้ iq8e → `empro959/line-slip-bot`~~ — เสร็จ 19 ส.ค. (ดูข้อ 5 ใน TL;DR)
2. 🟢 เปลี่ยนชื่อ Render `s772` กันหยิบผิด → ดู §3.6
3. ✅ ~~เปลี่ยนชื่อ monitor ใน UptimeRobot~~ — เสร็จ 19 ส.ค. ทั้ง `[ร้าน] LineSlipBot` และ `[E&M] ProEng`

> ข้อยุตินี้เจ้าของเคาะเองหลังชั่งน้ำหนักครบ — ห้ามหยิบมาตัดสินใหม่โดยไม่มีข้อเท็จจริงใหม่

### 3.6 🗓️ แผนเก็บกวาดของเก่า — "เปลี่ยนชื่อก่อน ลบสิ้นเดือน" (เจ้าของเคาะ 19 ส.ค. 2026)

**หลักที่ใช้ตัดสินว่าลบได้หรือยัง — ต้องครบ 3 ข้อ:**
1. ไม่มีเส้นเชื่อมไหนชี้มาที่มันแล้ว (ดู §3.4)
2. ข้อมูลข้างในมีสำเนาแล้ว
3. ระบบใหม่รันนิ่งมาแล้วอย่างน้อย ~2 สัปดาห์

**ทำวันนี้ (19 ส.ค.) — เปลี่ยนชื่อเท่านั้น ยังไม่ลบ:**

| ของ | ที่อยู่ | ทำอะไร |
|---|---|---|
| Render `Line-Slip-Bot` (URL ลงท้าย `s772`, Suspended) | Render บัญชีร้าน — **ระวังสับสนกับ `Line-Slip-Bot-1`/iq8e ที่เป็นตัวจริง** ยืนยันด้วย URL ต้องมี `s772` | ✅ **เปลี่ยนชื่อเป็น `DO-NOT-USE-old` แล้ว 19 ส.ค. 12:35** (ยืนยันหน้า service: srv-d9qrob9…, URL `line-slip-bot-s772`, Suspended by you — ไม่คิดเงิน) |
| monitor บอทร้าน | UptimeRobot **บัญชี E&M ProEngineering** (บัญชีเดียว ปนสองกิจการ) | ✅ ตั้งชื่อ `[ร้าน] LineSlipBot` แล้ว 19 ส.ค. |
| monitor เว็บ E&M | ที่เดียวกัน | ✅ ตั้งชื่อ `[E&M] ProEng` แล้ว 19 ส.ค. |

**นัดลบ 31 ส.ค. 2026 (ตั้งเตือนอัตโนมัติไว้แล้ว):**

| ของ | เงื่อนไขก่อนลบ |
|---|---|
| Render `DO-NOT-USE-old` (s772) | **ถ่ายภาพหน้า Environment เก็บก่อนเสมอ** (env คือของที่กู้คืนยากที่สุด) + ยืนยันว่า iq8e ยังรันปกติมาตลอดเดือน |
| Render `65gt` (**ของบอทร้าน ไม่ใช่ E&M**) | ไม่มีอะไรชี้มา ลบได้พร้อมกัน |
| Supabase project ของ `empro959` (pause อยู่) | export เก็บไฟล์แล้ว 18 ส.ค. — เก็บต่ออีกเดือนแล้วค่อยลบ (ฟรี ไม่รีบ) |
| GitHub `saiyangsoi-sketch/Line-Slip-Bot` | **ลบ/archive ไม่ได้ — ล็อกอินหาย** · ปล่อยไว้เฉยๆ ไม่มีอะไรดึงจากมันแล้ว |

> เหตุผลที่ไม่ลบทันที: ของที่ suspend อยู่**ไม่เสียเงินสักบาท** (ยืนยันจากใบแจ้งหนี้ Render — มีแต่ iq8e ที่คิดเงิน) การรอจึงไม่มีต้นทุน แต่การลบย้อนกลับไม่ได้

**⏰ ตั้งเตือนอัตโนมัติแล้ว:** Routine `trig_01EdP47dp2VUKPRznQEvhYKY` — ยิงครั้งเดียว **31 ส.ค. 2026 เวลา 11:00 น. (ไทย)** เข้าทั้ง push มือถือและอีเมล · ถ้าอยากเลื่อน/ยกเลิก บอก Claude ได้เลย (อ้าง trigger id นี้)

---

## 3.7 🩹 บั๊กชุดใหญ่ 19–20 ส.ค. 2026 — "ยอดสลิปไม่ตรง" (อ่านก่อนแตะระบบสลิป/จอง)

เริ่มจากคำถามเดียว *"ยอดสลิปกับยอดโอนไม่ตรง ให้บอทหาเองได้ไหม"* แล้วเจอบั๊กจริง 11 ตัว
**ทุกตัวมีเทสต์คุมแล้ว (91 เคส) — ห้ามถอดเทสต์ออก**

### เงินที่หายไปจริง (วันที่ 19/8 วันเดียว)

| สาเหตุ | จำนวน | เงิน |
|---|---|---|
| OCR อ่านชื่อร้านเพี้ยน (`ไส้ย่าง`→`ไส้บ้าง`/`ไยย่าง`) → ตัดว่า "ไม่ใช่รายรับ" | 2 ใบ | 1,017 |
| อ่านสลับด้าน — เอาข้อมูล **ฝั่งผู้โอน** มาเป็นผู้รับ | 3 ใบ | 5,429 |
| **รวมที่กู้คืนได้** | 5 ใบ | **6,446** |

ยอดจริง 19/8 = **60,057 บาท** (เดิมระบบเห็น 53,611)

### บทเรียนที่ใช้ได้ทั่วไป

1. **ระบบไม่พังแบบมี error — มันพังแบบเงียบ** ทุกเคสข้างบนไม่มี error สักตัว ตัวที่จับได้คือ **บรรทัดนับท้ายรายงาน** (`รับรูป 64 | บันทึกเป็นรายรับ 38 | ตกหล่น 14 | ข้ามไม่ใช่รายรับ 12`) → **ห้ามเอาบรรทัดนี้ออก**
2. **ข้อมูลผิดแย่กว่าข้อมูลว่าง** — บอทเดาธนาคารเป็น "ธนชาต" ทั้งที่เป็นใบกสิกร ทำให้เจ้าของไล่หาสลิปที่ไม่มีอยู่จริง · ตอนนี้กฎคือ **ไม่ชัด → ตอบ null ห้ามเดา**
3. **ห้ามเดาแทนในเรื่องเงิน** — ชื่อร้านโผล่ฝั่งผู้โอน = อาจอ่านสลับ **แต่ห้ามกลับข้างให้เอง** เพราะสลิป "ร้านจ่ายออก" หน้าตาเหมือนกันเป๊ะ → ติดธง ⚠️ ให้คนตัดสิน
4. **คำสั่งที่ใช้ตอนฉุกเฉิน ห้ามเรื่องมาก** — `เพิ่มสลิป` เดิมบังคับลำดับคำ ผิดนิดเดียวตอบ "วิธีใช้" กลับมา · ตอนนี้สลับลำดับได้ วางทีเดียวหลายบรรทัดได้

### เครื่องมือใหม่ที่ได้มา

| คำสั่ง/ระบบ | ทำอะไร |
|---|---|
| `กู้สลิป [วันที่]` | ย้อนอ่านรูปที่พลาดของวันนั้น แล้วบันทึกใบที่อ่านได้ให้เอง · **ลงวันเดิมของรูป ไม่ใช่วันที่กดกู้** · ใบที่เคยตัดว่า "ไม่ใช่รายรับ" กู้ได้จากบันทึกเดิมโดยไม่ต้องอ่านรูปซ้ำ (ไม่เสียค่า AI) |
| กู้อัตโนมัติ | รันเองก่อนส่งรายงานประจำวัน (`REPORT_RECOVER_MAX`=10 ใบ/รอบ, 0=ปิด) |
| `รายการข้าม <วันที่>` | ลิสต์ใบที่ถูกข้าม + ธง ⚠️ ถ้าสงสัยว่าอ่านสลับด้าน |
| เพิ่มสลิปหลายใบ | วางทีเดียวหลายบรรทัด บรรทัดถัดไปไม่ต้องพิมพ์คำสั่งซ้ำ |

### ⚠️ กับดักเวลาตั้ง env (เจอตอนแก้จริง)

`REPORT_HOUR`/`REPORT_MIN` เลื่อนเวลารายงานสลิปได้ (ตั้ง **5:30** เพื่อรอ POS ที่เข้าตี 1–5)
**แต่ช่วง "เงียบลึก" 03:00–10:00 เดิมปิดงานตามเวลาทั้งหมด** → ถ้าไม่แก้โค้ด ตั้งเวลาในช่วงนั้นแล้ว **รายงานจะไม่ออกเลย**
แก้แล้วด้วยการเปิดช่อง 45 นาทีนับจากเวลารายงาน (`_is_report_window`) — **ถ้าจะเลื่อนเวลาอีก ให้เช็คว่าตกในช่วงเงียบลึกไหม**

### ฝั่งจองโต๊ะ (แก้พร้อมกัน)

- ข้อความที่ **ไม่มีคำว่า "จอง"** เคยถูกตัดทิ้งเงียบ → ตอนนี้ดู "รูปแบบ" แทน (นับสัญญาณ จำนวนคน/โซน/เวลา/วันที่ เจอ 2 อย่างขึ้นไปถึงอ่าน)
- ข้อมูลไม่ครบ → **ถามแบบคนคุยกันทีละข้อ** แล้วเอาคำตอบสั้นๆ ('12') มาต่อให้เอง (เดิมให้พิมพ์ใหม่ทั้งชุด + แปะคู่มือยาว)
- เบอร์โทรในข้อความจองเคยถูกอ่านเป็นเวลา/จำนวนคน → เพิ่มกฎแล้ว

---

## 3.8 🩹 บั๊กชุด 22–24 ส.ค. 2026 — 13 ตัว (อ่านก่อนแตะระบบสลิป/จอง/หนี้)

> ทั้งชุดมาจาก **หน้างานจริง** เจ้าของส่งภาพหน้าจอมาทีละเคส · ทุกตัวมีเทสต์ยึงไว้แล้ว

### 🟥 กลุ่มที่ทำให้ "เงินผิด"
| # | อาการที่เห็น | ต้นเหตุ | commit |
|---|---|---|---|
| 1 | สลิป 873 (ไส้ย่างซอย4 2481 → พิมนภัทร์&สุวัฒน์ 4612) ถูกนับเป็นรายรับ | กติกาดูแค่ "ปลายทางตรงบัญชีร้านไหม" ไม่เคยดูต้นทาง → **โอนระหว่างกระเป๋าตัวเอง = นับซ้ำ** | `2f7d08e` |
| 2 | โอน 9,027 โน้ต "(19/8 และ 21/8) 2 บิล" ไปตัดบิลใหญ่ 21,408 | `_date_from_memo` ใช้ `re.search` เอาวันแรกวันเดียว | `2ad83af` |
| 3 | โอน 20,297 โน้ต "(22,23/8)" ไม่แตะบิล 22/8 เลย | `22` ไม่มี `/เดือน` ของตัวเอง → regex มองไม่เห็น | `251718b` |
| 4 | สลิป 2,075 ถูกฟันธง "🚨 ตัดต่อ" ทั้งที่คนละคนคนละวัน | AI หยิบ **เลขพร้อมเพย์ปลายทาง** `XXXX…5949` มาใส่ช่อง `ref_number` → ลูกค้าทุกคนที่โอนเข้าพร้อมเพย์เดียวกันได้ "เลขอ้างอิง" เดียวกัน | `60a2e28` |

### 🟧 กลุ่ม "เงียบ / พังโดยไม่มีใครรู้"
| # | อาการ | ต้นเหตุ | commit |
|---|---|---|---|
| 5 | ก๊อป "สรุปหนี้" มาวางกลับ → บอทเงียบสนิท | ทุกบรรทัดเป็นบิล ไม่มี "ยกมา" → `entries` ว่าง → ข้อความตอบหยิบ `entries[0]` → **IndexError** · บิลถูกบันทึกไปแล้วแต่คำตอบไม่เคยถูกส่ง | `31d03b3` |
| 6 | ผลกู้สลิปไม่พูดถึงใบที่กู้ไม่ได้ | SQL กรองใบไม่มีรหัสรูปทิ้งตั้งแต่ต้น | `2f7d08e` |

### 🟨 กลุ่ม "กวน / ตอบผิดใส่หน้าคน"
| # | อาการ | ต้นเหตุ | commit |
|---|---|---|---|
| 7 | คุยเรื่องขายของถูกตีเป็นจอง + ถามซ้ำการ์ดเดิม | `ลูกค้า` เป็นคำใบ้ที่อ่อนมาก | `10f2ba3` |
| 8 | "พี่ผมขอเคลียร์ยอดค้าง…" → ตอบ "❌ อ่านบล็อกค้างไม่ได้" | เช็คแค่ว่ามีคำว่า "ค้าง" ในบรรทัดแรก | `f608486` |
| 9 | ส่ง 2 รูปพร้อมกัน → การ์ดสรุปยาวๆ เด้ง 2 ใบ | ส่งทันทีทุกใบ | `cdf48fb` |

### 🟦 กลุ่มจองโต๊ะ
| # | อาการ | ต้นเหตุ | commit |
|---|---|---|---|
| 10 | จองพิมพ์ทีละท่อน (`@All b10` / `คุนนาต` / `มาทุ่ม`) เงียบทั้งชุด | ทุกท่อนได้ 0 สัญญาณ (รหัสโต๊ะลอยๆ กับ "ทุ่ม" ไม่มีเลขนำหน้า ไม่ถูกนับ) | `f8abb25` |
| 11 | `สรุปจอง` ตอบ "ไม่มีการจอง" ทั้งที่มี | กรองเฉพาะจองที่เกิด/ถูกส่งเข้ากลุ่มที่พิมพ์ — **ร้านมีชุดโต๊ะชุดเดียว** | `4e15b86` |
| 12 | บอทถาม 19:22 พนักงานตอบ 21:12 → จองหลุด | ร่างจองหมดอายุ 15 นาที (หน้าร้านตอบตอนว่าง) → **180 นาที** | `8fd63b3` |
| 13 | ถามโซนทั้งที่มี `A4` · `2 ท่าน ท่าน` · วันเพี้ยน · เลขโต๊ะกลายเป็นจำนวนคน | ปล่อยให้ AI ทำเรื่องที่กติกาตายตัวอยู่แล้ว | `7bb9e8a` `e2f052a` `f7991a4` |

### 🧭 หลักการที่ใช้ซ้ำทั้งชุด (สำคัญกว่าตัวบั๊ก)
1. **เรื่องที่กติกาตายตัวอยู่แล้ว ห้ามฝากไว้กับ AI** — วันที่ที่คนเขียนมาตรงๆ · รหัสโต๊ะ · เลขอ้างอิงที่ถูกปิดบัง → แกะด้วย regex ในโค้ด แล้วทับค่าที่ AI ให้มา (prompt เป็นแค่ชั้นแรก)
2. **เงียบอันตรายกว่า error** — ทุกทางที่ `return` โดยไม่ตอบอะไร ต้องถามตัวเองว่า "คนสั่งจะรู้ไหมว่าเกิดอะไรขึ้น"
3. **เรื่องเงินห้ามเดา** — เจอมากกว่า 1 ความเป็นไปได้ = ไม่ทำ แล้วบอกให้คนตัดสิน (เช่น `_exact_combo` เจอ 2 ชุดที่บวกได้เท่ากัน → คืน `None`)
4. **ยอมถามเกิน ดีกว่าบันทึกผิด** — จำนวนคนผิด = เตรียมโต๊ะผิด · ถามเพิ่ม 1 คำถามเสียเวลา 5 วินาที
5. **คำเตือนที่ผิดบ่อย = คำเตือนที่ไม่มีใครเชื่อ** — false positive ของตัวจับสลิปปลอมแพงที่สุดในระบบ
6. **ป้ายเวลาสัมพัทธ์ต้องมีเลขจริงกำกับ** — "พรุ่งนี้" อ่านตอนไหนก็เปลี่ยนความหมาย

### ⚠️ กับดักที่เพิ่งโดน (อย่าซ้ำ)
- **`ลบจ่ายล่าสุด` สั่งซ้ำ 2 รอบ** → ลบทั้ง 20,297 และ 10,000 ยอดหนี้เกินจริง 10,000
  → ตอนนี้มีปุ่มยืนยันแล้วถ้าสั่งซ้ำใน 120 วิ (`260a367`) แต่**ครั้งแรกยังลบทันที**
- **`ล้างบัญชีหนี้` ลบประวัติการจ่ายด้วย** — ยอดค้างถูก แต่ประวัติหาย (เจ้าของบอกว่าไม่เป็นไร ดูย้อนในแชทได้)
- **push ทั้งที่เทสต์แดง** เกิดขึ้นอีกครั้ง (`60a2e28`) เพราะ chain คำสั่งผิด →
  ใช้ `python -m unittest ... | tail -3 | grep -q "^OK" && git commit ...` เสมอ

---

## 3.9 🩹 ใบแจ้งรายการ (บิลกระดาษ) ถูกนับเป็นรายรับซ้ำ — 30 ส.ค. 2026 ✅ merge + deploy แล้ว 2 ก.ย.

> เดิมอยู่ในลิสต์ ⚪ "เสนอไว้ รอเจ้าของสั่ง" ข้อ 1 · ไล่ของจริงแล้วพบว่า**เกิดขึ้นได้จริง ไม่ใช่แค่ความเสี่ยง**

**อาการ:** บิลร้าน (ใบแจ้งรายการ) ที่ถ่ายส่งเข้ากลุ่มรับสลิป ถูกบันทึกเป็น "รายรับ"
ซ้ำกับสลิปที่ลูกค้าโอนจริงของโต๊ะเดียวกัน → ยอดรับของวันนั้นเกินจริง **เงียบสนิท**

**ต้นเหตุ 3 ชั้นซ้อนกัน:**
1. คำสั่งอ่าน `is_bill` ถูกใส่ใน prompt **เฉพาะกลุ่มใน `DINING_GROUPS`** — ตอนตรวจมี
   **3 ใน 4 กลุ่มรับสลิปที่ไม่ได้เปิด** จึงไม่เคยถูกถามเลยว่ารูปนี้เป็นบิลไหม
2. กฎกันพลาด *"ถ้า AI อ่านยอดได้ ให้ถือเป็นสลิปเสมอ"* (เขียนไว้ตอนไล่บั๊ก 19/8)
   **ทับคำตอบ `is_slip=false` ที่ถูกต้องของ AI ทิ้ง** → บิลกลายเป็นสลิป
   แล้วชื่อร้านบนหัวบิลก็ไปตรง `PAYEE_ACCOUNTS` พอดี = "รายรับ"
3. **ตัวจับซ้ำไม่มีทางจับได้เลย** — `find_duplicate` ต้องมีทั้ง `amount` และ `slip_datetime`
   ตรงกัน แต่บิลไม่มีเวลาโอน จึงไม่เข้าเงื่อนไขสักทาง แม้ลูกค้าจะโอนตรงยอดบิลเป๊ะ

**เส้นทางที่สอง (อันตรายกว่า):** ตัวกู้สลิป — ทางลัด "ตัดสินใหม่จาก detail" ยัด `is_slip=True`
ให้เอง และ prompt รอบสองเอนไปทาง `is_slip=true` อยู่แล้ว → บิลที่เคยข้ามถูกแล้ว
ถูกดึงกลับมาเป็นรายรับได้ · **กู้อัตโนมัติรันเองก่อนส่งรายงานประจำวัน** = เกินทุกวันโดยไม่มีใครสั่ง

**แก้:**
- ถาม `is_bill`/`bill_total` **ทุกกลุ่ม** (`bill_table` ยังถามเฉพาะกลุ่มที่ต้องจับคู่)
- ด่าน `_is_store_bill()` วางไว้ **ก่อน** กฎ "มียอด = ถือเป็นสลิป" — กลุ่ม dining เข้าคิวจับคู่ตามเดิม
  กลุ่มอื่นบันทึกเป็น `notincome` ติดป้าย `_BILL_MARK` (เห็นได้ที่ `รายการข้าม`)
- ตัวกู้สลิปข้ามแถวที่ติดป้ายนั้น + เช็คซ้ำหลังอ่านรูปใหม่ (แบบเดียวกับ `_INTERNAL_MARK`)

**กติกาที่ยึด:**
- 🔴 **ด่านนี้แคบไว้ก่อน** — ต้อง AI ชี้ว่า "เป็นบิล" **และ** "ไม่ใช่สลิป" พร้อมกันเท่านั้น
  **ห้ามเดาจาก "ไม่มีเลขอ้างอิง/ไม่มีชื่อผู้โอน"** — สลิปจริงที่ OCR อ่านไม่ครบหน้าตาแบบนั้นเป๊ะ
  แล้วจะกลายเป็นตัดเงินลูกค้าทิ้ง ซึ่ง**แพงกว่านับเกิน** (19/8 เคยตัดทิ้ง 1,017 บาทมาแล้ว)
- 🔴 **ต้นฉบับเดียว** (`_is_store_bill`) ใช้ทั้งเส้นรับรูปและเส้นกู้สลิป — สองเส้นที่ต้องให้ผล
  เหมือนกันแต่คนละโค้ด จะแยกจากกันวันใดวันหนึ่ง
- ⚠️ **เทสต์คุมสองทิศเสมอ** (`tests/test_store_bill.py` 11 เคส): บิลต้องไม่เข้า · สลิปจริงต้องไม่ถูกตัด
  · พิสูจน์แล้วว่า**ตกจริง 9/11 เมื่อย้อนโค้ดกลับ**

⚠️ **กับดักที่เพิ่งโดนตอนเขียนเทสต์:** `app.py` อ่าน env ตอน import และ Python import โมดูลครั้งเดียว
→ ไฟล์เทสต์ที่รันก่อนหน้า import `app` ไปแล้วด้วย env ของตัวเอง · ตั้ง `os.environ` ในไฟล์เทสต์ใหม่
จึง**ไม่มีผล** ข้อจะเขียวตอนรันไฟล์เดียวแต่แดงตอนรันทั้งชุด → **ตั้งค่าที่ตัวโมดูล (`app.PAYEE_*`) ใน `setUp` แทน**

### ⚠️ ระดับการตรวจ — merge + deploy แล้ว (`fd622db` · GitHub Actions run #65 `success`)
**ยังไม่เคยตรวจบน production จริง** — พิสูจน์ได้แค่ระดับ unit test บนเครื่อง dev เท่านั้น
ยังไม่เคยลองกับรูปบิลจริงผ่าน Gemini ของจริง (ห้อง Code ยิงเน็ตออกไม่ได้)
→ เมื่อมีจังหวะ ถ่ายบิลจริง 1 ใบส่งเข้ากลุ่มรับสลิปที่ไม่ใช่กลุ่ม dining แล้วเช็คว่าขึ้นใน
`รายการข้าม` ว่าเป็นบิล ไม่ใช่เข้า `สรุป` เป็นรายรับ

---

## 3.10 ⏰ รายงาน POS เข้า "ก่อนปิดร้าน" — 30 ส.ค. 2026 ✅ ยืนยันแล้ว

> เจ้าของสังเกตเห็นเองว่า *"วันนี้มีอีเมลเด้งมาก่อนปิดกะ"*

### ข้อเท็จจริง (เจ้าของยืนยัน 30 ส.ค.)
- หัวเรื่อง **"รายงานยอดรายวันSYS4 30/08/2569"** · เข้ามา **21:52** (ปกติ POS ส่ง ~00:3x หลังปิดร้าน)
- **ไฟล์แนบมาครบทุกชนิด** รวม `SaleReport` + `PayoutReport` → เข้าเงื่อนไขนำเข้าเต็มตัว
- **เป็นยอดของวันที่ 30/08 ที่พิมพ์ตอนวันยังไม่จบ** (ไม่ใช่ใบกู้ของ 29/08 — dashboard มีข้อมูลครบทุกวันอยู่แล้ว)

### ทำไมถึงอันตราย — ไล่โค้ด `dashboard/Code_full.gs` แล้ว
รายงานที่พิมพ์ตอน 3 ทุ่มครึ่งขณะร้านยังเปิด = **ยอดขายยังไม่จบวัน**
`importPosReports` (trigger ตี1–5) หยิบ **ใบล่าสุดเสมอ** แล้วอ่านวันที่จาก *เนื้อใน* PDF → บันทึกเป็น "ยอดวันที่ 30/08"

- 🔴 **`_rangeWarn_` จับไม่ได้** — มันเตือนเฉพาะใบที่ครอบ **หลายวัน** (`days<2` คืนค่าว่าง)
  ใบนี้เป็นวันเดียวถูกต้อง แค่ยังไม่จบวัน → **ผ่านฉลุย ไม่มีคำเตือนสักตัว** (ตระกูล "ยอดขายผิดแบบเงียบ" §6.1 ของ dashboard)
- 🔴 **แก้ทีหลังแล้วไม่มีใครรู้** — ใบเต็มที่ตามมาจะเขียนทับให้ถูกเอง (`daily.filter(x=>x.date!==dd.key)`)
  **แต่สรุปเข้า LINE จะไม่ส่งซ้ำ** เพราะติด `if(props.getProperty('LAST_ALERT_DATE')===dd.key) return;`
  → เจ้าของเห็นเลขต่ำกว่าจริงใบเดียว ส่วนเลขที่ถูกเงียบอยู่บน dashboard โดยไม่มีอะไรบอกว่ามันเปลี่ยนแล้ว
- 🔴 **`findLatestPdf_` เลือกไฟล์แต่ละชนิดแยกกันตามความใหม่** — ไม่เคยเช็คว่ามาจาก
  **เมลฉบับเดียวกัน** หรือ **วันเดียวกัน** · เมลนอกตารางที่มีไฟล์มาไม่ครบชนิด คือ input ที่ทำให้มันจับคู่ข้ามวัน
  (ยอดวิธีชำระ/โซน/บิล ของคืนนี้ ไปเกาะกับ `SaleReport` ของเมื่อวานได้)
- ⚠️ `backfillPos` สมมติว่า **"ชื่อไฟล์ = วันเนื้อหา +1 (พิมพ์เช้าวันรุ่งขึ้น)"** — ใบที่พิมพ์วันเดียวกับเนื้อหา
  ทำให้ `haveSuf` คำนวณผิด → อาจ OCR ซ้ำเปลืองโควตา (ไม่กระทบเงิน)

### 🔴 นำเข้าได้ "วันเดียวต่อรอบ" — เมลสองฉบับในคืนเดียว ฉบับเก่ากว่าหายเงียบ
trigger รัน **ตี 1, 2, 3, 4, 5** · แต่ละรอบหยิบ `SaleReport` **ที่ใหม่ที่สุดตัวเดียว** แล้วจำเวลาไว้ที่
`LAST_IMPORTED_EMAIL_MS` → รอบถัดไปที่เจอเมลเดิมจะข้าม (`if(s.time && s.time<=lastMs) return;`)
**ถ้าคืนไหนมีเมล POS สองฉบับคนละวัน ฉบับที่เก่ากว่าจะไม่ถูกนำเข้าเลย** และไม่มีอะไรฟ้อง
→ กรณีนี้รอดเพราะทั้งสองใบเป็นวันเดียวกัน (30/08) แต่ **ถ้าใบ 21:52 เป็นใบกู้ของวันก่อนหน้า วันนั้นจะหายทั้งวัน**

### ทางแก้เฉพาะหน้า (ใช้กับเคสนี้)
1. **หลังปิดร้าน สั่ง POS ส่งรายงานของวันนั้นใหม่ ให้เข้าก่อนตี 5** — รอบตี 3/4/5 จะเห็นใบใหม่กว่า
   แล้วเขียนทับใบที่ยังไม่จบวันให้เอง (`daily.filter(x=>x.date!==dd.key)`) → **ตัวเลขบน dashboard จะถูกต้อง**
2. 🔴 **แต่สรุปที่เด้งเข้า LINE คืนนั้นจะเป็นเลขของใบ 21:52 (ต่ำกว่าจริง) และจะไม่ถูกส่งแก้**
   เพราะ `LAST_ALERT_DATE` ถูกตั้งไปแล้วตั้งแต่รอบแรก → **อย่าเชื่อเลขในไลน์คืนนั้น ให้ดู dashboard แทน**
3. ไม่ตรงอีกให้สั่ง `importPosByDate()` (เลือกอีเมลจากวันที่ ไม่ได้เลือกจากความใหม่)
   · `debugReports()` ไล่ดูว่ามีไฟล์รายงานวันไหนในเมลบ้าง **ไม่ยิง OCR ไม่กินโควตา**

### ทางแก้ถาวรที่เสนอ (ยังไม่ได้ทำ รอเจ้าของสั่ง)
- บังคับให้ไฟล์ทุกชนิดในรอบนำเข้าเดียวกัน **มาจากเมลฉบับเดียวกัน** (หรืออย่างน้อยวันเนื้อหาตรงกัน)
- เตือนเมื่อรายงานถูก **พิมพ์ก่อนปิดวัน** (เทียบเวลาที่เมลเข้า/เวลาบนใบ กับเวลาปิดร้าน) — ตอนนี้ไม่มีด่านนี้เลย
- ปลดล็อกให้แจ้งเตือนซ้ำได้เมื่อตัวเลขของวันนั้น **เปลี่ยนไปจากที่เคยแจ้ง** (ไม่ใช่ปิดตายด้วย `LAST_ALERT_DATE`)

---

## 3.11 👁️ ใครเห็นเมลอะไร — กันเข้าใจผิดซ้ำ (30 ส.ค. 2026)

**ระบบกับห้อง Code เห็นคนละกล่อง และเรื่องนี้เคยทำให้สรุปผิดมาแล้วในวันเดียวกัน**

| | อ่านกล่องไหน | ได้อะไร |
|---|---|---|
| **Apps Script (ระบบ)** | **saiyangsoi (ร้าน)** — สคริปต์รันอยู่ในบัญชีนั้น อ่านด้วย `GmailApp.search()` ตรงๆ | รายงาน POS ทุกคืน **ทำงานปกติ ไม่ต้องเชื่อมอะไร** |
| **ห้อง Code (แชท)** | **empro959 (E&M)** บัญชีเดียว | ไม่เห็นเมลของร้านเลยสักฉบับ |

- ✅ "ผมเปิดกล่องร้านไม่ได้" **ไม่ได้แปลว่าระบบพัง** — คนละเรื่องกันคนละชั้น
- อยากให้ห้อง Code ดูเมลร้านเอง → ต้องเชื่อมบัญชี saiyangsoi เพิ่ม · ไม่งั้น **ส่งภาพ/ไฟล์เข้าแชทก็พอ** (ใช้ได้จริง เคสนี้มาจากภาพหน้าจอล้วนๆ)

🔴 **ไส้ย่างซอย๔ ≠ E&M ProEngineering — คนละกิจการ** (เจ้าของยืนยัน 30 ส.ค.)
แยกกันหมด: คนละ repo · คนละ OA · คนละ DB · คนละ Drive · คนละ Render service (ดู `TEAM.md`)
เมล/ใบกำกับภาษีที่เข้ากล่อง empro959 เป็นงาน E&M **ห้ามเอามาปนกับบัญชี/ยอดของร้าน**
⚠️ `CLAUDE.md` บรรทัด 33 ยังเขียนว่า *"for ไส้ย่างซอย๔ (E&M)"* ซึ่ง **อ่านผิดได้ว่าเป็นกิจการเดียวกัน**
และเป็นไฟล์แรกที่ทุก session ใหม่อ่าน — **เสนอแก้เป็น** `(ร้านอาหาร — คนละกิจการกับ E&M ProEngineering)` รอเจ้าของเคาะ

---

## 3.12 💸 ค่าใช้จ่าย 1 สัปดาห์ ถูกนับ 7 รอบ — 30 ส.ค. 2026 ✅ แก้แล้ว (**รอ deploy มือ — ดูท้ายหัวข้อ**)

> เจ้าของอ่านสรุปรายสัปดาห์แล้วทักว่า *"ยอดไม่ถูกอ่ะ"* — ตรวจแล้วผิดจริง

### ตัวเลขที่เจอ
```
💰 ยอดขายรวม: ฿534,275
💸 ค่าใช้จ่ายรวม: ฿510,988   ← 96% ของยอดขาย เป็นไปไม่ได้
✅ กำไร: +฿23,287
```
สูตรถูกหมด (534,275 − 510,988 = 23,287 · 534,275 ÷ 7 = 76,325 เป๊ะ) → **เลขตั้งต้นผิด ไม่ใช่สูตรผิด**
ยอดขาย 76,325/วัน สมเหตุสมผล (ตรงกับที่เคยบันทึก 16/07 = 76,415) · **ตัวที่ผิดคือค่าใช้จ่าย**

### ต้นเหตุ — ยืนยันแล้วทั้งจากเจ้าของและจากโค้ด
**ใบจ่ายเงิน (PayoutReport) ที่ POS ส่งมา ครอบ 1 สัปดาห์** (เจ้าของยืนยัน) แต่ระบบลงเป็น "ค่าใช้จ่ายของวันเดียว"

และ `findLatestPdf_('PayoutReport')` **หยิบใบใหม่สุดโดยไม่เช็ควันเลยสักบรรทัด** —
ไม่มีที่ไหนเทียบว่าวันของใบจ่ายเงินตรงกับใบขายไหม (วันมาจากใบขายอย่างเดียว):
```js
var s=findLatestPdf_('SaleReport'), p=findLatestPdf_('PayoutReport');
var dd=dailyDate_(st)||dailyDate_(pt);
```
→ ตราบใดที่ยังไม่มีใบจ่ายเงินใบใหม่ **ทุกคืนหยิบใบสัปดาห์ใบเดิมมาใช้ซ้ำ แล้วเขียนลงทุกวัน**
→ **72,998 × 7 = 510,988** ตรงกับที่เห็นพอดี

### 🔴 ทำไมไม่มีอะไรฟ้อง — ตัวตรวจถูกเรียกไม่ครบ และสองเส้นทางไม่เหมือนกัน
ตัวตรวจ `_rangeWarn_` ("ใบนี้ครอบ N วัน แต่ถูกบันทึกเป็นวันเดียว → **ตัวเลขเกินจริง**") **มีอยู่แล้ว**
แต่ถูกเรียกต่างกันสองเส้น:

| เส้นทาง | ตรวจใบไหน |
|---|---|
| `importPosByDate` → `_importPosOneDate_` (กู้ย้อนหลัง สั่งเอง) | `_rangeWarn_(bt) \|\| _rangeWarn_(pt)` ← **ตรวจใบจ่ายเงินด้วย** |
| **`importPosReports` (รันเองทุกคืน ตี 1–5)** | `_rangeWarn_(st)` ← **ตรวจแต่ใบขาย** |

**เส้นที่ผลิตตัวเลขชุดนี้คือเส้นที่การ์ดอ่อนกว่า** — บทเรียนเดิมของโปรเจกต์:
สองเส้นที่ควรให้ผลเหมือนกันแต่คนละโค้ด จะแยกจากกันวันใดวันหนึ่ง

### ✅ ป้าย "กำไร"/"ค่าใช้จ่าย" — เจ้าของยืนยันแล้ว 2 ก.ย. ไม่ต้องเปลี่ยน (ปิดคำถามนี้)
ห้อง Code เคยเสนอเปลี่ยนป้ายเป็น "เงินจ่ายออกจากลิ้นชัก" เพราะกลัวว่า `sales - PayoutReport`
ไม่ใช่กำไรที่แท้จริง (ไม่รวมค่าแรง/ค่าเช่า) → **เจ้าของแก้ให้ตรง: "เงินจ่ายออกจากลิ้นชักก็ลงบัญชี"**
ยอดในใบ PayoutReport ไม่ใช่แค่เงินเดินสะพัดที่ไม่มีความหมายทางบัญชี แต่เป็นรายจ่ายที่ถูกบันทึกจริง
→ **ป้ายเดิม "กำไร"/"ค่าใช้จ่าย" ถูกต้องแล้ว ไม่ต้องเปลี่ยน** — โค้ดที่ commit ไปก็ไม่เคยแตะป้ายนี้อยู่แล้ว
(แก้แค่กรณี "ไม่ทราบ" ให้ขึ้น `—` เท่านั้น ไม่ได้เปลี่ยนคำของกรณีปกติเลยสักตัว) — ปิดคำถามนี้ถาวร
ไม่ต้องหยิบมาถามใหม่ (ยังไม่รวมค่าแรง/ค่าเช่าอยู่ดีในความหมายที่ "กำไร" ไม่ใช่กำไรสุทธิเป๊ะ 100% —
แต่นั่นเป็นข้อจำกัดของสิ่งที่ POS รายงานมาให้ ไม่ใช่ป้ายที่ตั้งผิด)

### ✅ แก้แล้ว (2 ก.ย. — commit บน `fix/weekly-expense-payout-range`)
1. **ด่านที่ต้นฉบับเดียว**: `payoutRangeIssue = _rangeWarn_(pt)` — ใบ Payout ครอบมากกว่า 1 วัน
   → `expenses = null` ('ไม่ทราบ' ไม่ใช่ 'ศูนย์') แทนการลงเป็นค่าใช้จ่ายของวันเดียว
   ใส่ใน **ทั้งสองเส้นทางนำเข้า** (`importPosReports` ที่รันทุกคืน + `_importPosOneDate_` ที่กู้ย้อนหลัง)
   ด้วยเงื่อนไขเดียวกัน — ของเดิม `_importPosOneDate_` มี `rangeWarn` อยู่แล้วแต่**ต่อท้าย log เฉยๆ ไม่เคยกันข้อมูลจริง**
2. `daySum_` คืน `expKnown` เพิ่ม — ตัวเรียกทุกตัวต้องเช็คก่อนเชื่อเลข `exp`
3. `buildDailyMsg_`/`buildWeeklyMsg_` (ข้อความที่ส่งเข้า LINE จริง) — วันไหน/สัปดาห์ไหนมีวันที่ไม่ทราบ
   ค่าใช้จ่าย ขึ้น **`—` พร้อมเหตุผล** แทนตัวเลข ไม่ใช่ 0 หรือของซ้ำ
4. `profitByWeekday()` (เครื่องมือมือ) + `buildWeekdayProfit()` (กราฟบนเว็บ `saisang_dashboard.html`)
   — วันที่ไม่ทราบค่าใช้จ่ายไม่ถูกเอาเข้าเฉลี่ย (ไม่งั้นกำไรของวันนั้นดูดีเกินจริงเพราะหารด้วย "ต้นทุน 0")
5. `rebuildMonths_` (JSON ที่เลี้ยงกราฟเว็บ) — `daily_totals[].expenses/profit/raw/bevc` เป็น `null` ในวันที่ไม่ทราบ
   (Chart.js อ่าน `null` เป็น "ไม่มีจุดข้อมูล" คือช่องว่างในกราฟ ไม่ใช่จุดที่ค่าเป็นศูนย์) ·
   เพิ่ม `expenses_incomplete_days` ต่อเดือน ให้รู้ว่ายอดรวมเดือนเป็น "ขั้นต่ำเท่าที่รู้" เมื่อ >0
6. **ข้อ 5 เดิม (ข้อมูลเก่าที่เพี้ยนแล้วต้องนำเข้าใหม่)** — ใช้ `debugWeekly()` ที่ส่งให้แล้ว (ดูด้านล่าง)
   ระบุวันที่ต้องกู้ แล้วสั่ง `importPosByDate()` ซ้ำ ไม่ได้ทำเครื่องมือกู้อัตโนมัติเพิ่ม (ไม่จำเป็น มีอยู่แล้ว)

### 🔴 ทดสอบยังไง — Apps Script รันในเครื่องไม่ได้ ไม่มีชุดเทสต์แบบ `app.py`
พิสูจน์ด้วยการโหลดทั้งไฟล์ผ่าน Node `vm` (เหมือนกัน API กับ browser/V8 ที่ Apps Script ใช้)
แล้วเรียกฟังก์ชันจริงด้วยข้อมูลจำลอง — **ไม่ใช่การรันบน Apps Script ของจริง**:
- `daySum_`: `expenses=null` → `expKnown:false` · `expenses=[]`/`undefined` → ทำงานเหมือนเดิมทุกกรณี
- จำลองสัปดาห์ที่ทุกวัน `expenses=null` (เคสจริง) → `buildWeeklyMsg_` **ไม่มีเลข 510,988 โผล่มาเลย**
  ขึ้น "ไม่ทราบ 7/7 วัน" แทน · ยอดขาย 534,275 ยังถูกต้องเป๊ะ
- จำลองสัปดาห์ปกติ (ไม่มีบั๊ก) → คำนวณค่าใช้จ่าย/กำไรถูกต้องตามเดิมทุกตัวเลข (ไม่กระทบเคสปกติ)
- จำลองสัปดาห์ผสม (3 วันไม่ทราบ + 4 วันรู้) → ขึ้น "ไม่ทราบ 3/7 วัน" ไม่ใช่ยอดต่ำกว่าจริงเงียบๆ
- `_rangeWarn_` กับข้อความจริงจากรูปแบบ POS: ใบสัปดาห์ (26 ส.ค.–1 ก.ย.) ถูกจับ ✅ ·
  ใบวันเดียวและใบข้ามคืน (30–31 ส.ค.) **ผ่านฉลุยตามปกติ** — ยืนยันว่าไม่มี false positive กับการนำเข้าปกติ
- ตรวจ syntax ทั้งไฟล์ (`.gs` และ `<script>` ในไฟล์ HTML) ด้วย `node -e "new Function(...)"` ผ่านทุกจุดที่แก้

⚠️ **ระดับการตรวจ: จำลองด้วย Node เท่านั้น ไม่เคยรันบน Apps Script ของจริง**
ยังไม่เคยยิง trigger จริง ยังไม่เคยลองกับ PDF จริงจาก POS — ต้องรอรันจริงหลัง deploy ยืนยันอีกที

### 🔴 merge PR แล้ว ≠ deploy แล้ว — ต้องมีคนก๊อปวางมือ
`dashboard/Code_full.gs` **ไม่มี clasp/CI ผูกกับ Apps Script ของจริงเลย** (เช็คแล้ว ไม่มี `.clasp.json`
ไม่มี workflow ไหนยิงเข้า `script.google.com`) — เป็นแค่**สำเนาอ้างอิงในรีโป**
→ merge PR เข้า `main` **ไม่ทำให้โค้ดที่รันจริงเปลี่ยนเลย** ต้องมีคน:
1. เปิด Apps Script ที่ผูกกับ `pos_daily.json`/`saisang_data.json`
2. เอาโค้ดใน `dashboard/Code_full.gs` (หลัง merge) ไปวางทับทั้งไฟล์ แล้วกด Save
3. `saisang_dashboard.html` (กราฟ "กำไร/ขาดทุนเฉลี่ยต่อวัน") ก็ต้องอัป Netlify ใหม่ (ลาก zip — deploy มือเหมือนเดิม)
   ⚠️ ไม่อัปก็ไม่พังอะไร (โค้ดเดิมยังรันได้ แค่ยังไม่กันวันที่ไม่ทราบค่าใช้จ่ายในกราฟนั้นจุดเดียว)

📋 **2 ก.ย. — ขั้นตอนแบบคลิกต่อคลิกทั้ง 2 ข้อ (Apps Script + Netlify) เขียนไว้ครบแล้วที่**
`dashboard/DEPLOY_STEPS_2026-09-02.md` (มีขั้นตอนสำรองโค้ดเดิมก่อนทับด้วย) — ให้ Cowork หรือเจ้าของ
เปิดไฟล์นั้นทำตามได้เลย ยังไม่มีใครทำจริง ณ เวลาที่เขียน (2 ก.ย. 20:26)

⚠️ **`.github/workflows/render-deploy.yml` ไม่มี path filter** — push/merge ขึ้น `main`
ก็ยัง**ยิง Deploy Hook รีสตาร์ทบอทเหมือนเดิม** ทั้งที่ไฟล์ที่แก้ไม่เกี่ยวกับ `app.py` เลย
→ กติกาเดิมยังใช้: **merge ตอนร้านปิดเท่านั้น**

### เครื่องมือที่ทำไว้ให้แล้ว (ไม่ต้องทำเพิ่ม)
`debugWeekly()` (ส่งให้เจ้าของแล้ว 30 ส.ค. · ยังไม่ได้เข้ารีโป) — อ่านอย่างเดียว ไม่ยิง OCR ไม่กินโควตา:
ยอดขาย/ค่าใช้จ่ายรายวันของ 7 วันที่สรุปใช้จริง · **จับว่าชุดค่าใช้จ่ายซ้ำข้ามวันไหม** ·
10 รายการจ่ายที่ใหญ่สุด (ดูว่าข้างในคืออะไรจริง ๆ) · ธงรายการเดี่ยวที่ใหญ่เกิน 30% ของยอดขายวันนั้น
→ ใช้ระบุว่าวันไหนในอดีตที่ยังเก็บข้อมูลเพี้ยนอยู่ (`expenses` เป็นก้อนซ้ำ ไม่ใช่ `null`) แล้วสั่ง
`importPosByDate()` ใส่วันนั้นซ้ำ (ต้องมีใบ Payout วันเดียวจริงในเมลก่อนถึงจะกู้ได้)

---

## 3.13 🩹 ลบบิลผิดใบไม่ได้ตรง ๆ + คำสั่งไม่รู้จักในกลุ่มหนี้เงียบสนิท — 30 ส.ค. 2026 ✅ merge + deploy แล้ว 2 ก.ย.

> เจ้าของพิมพ์ `ลบบิล2/9 18795` แล้ว `ลบบิล 2/9 18795` (2 ครั้ง) ในกลุ่มดวงใจ — **บอทไม่ตอบเลยทั้งสองครั้ง**
> (ภาพหน้าจอ: ขึ้น "อ่านแล้ว" แต่ไม่มี reply ใด ๆ ต่อท้าย)

### เกิดจากอะไร — ยืนยันจากโค้ดแล้ว
`ลบบิล <วันที่> <ยอด>` **ไม่มีอยู่ในระบบ** คำสั่งลบที่มีจริงคือ:
- `ลบบิลล่าสุด`/`ลบบิล` — ต้อง**พิมพ์เป๊ะแค่นั้น** ห้ามมีวันที่/ยอดต่อท้าย (`low in (...)` เทียบตรงตัว)
- `ลบวันที่ <วันที่>` — ลบ**ทุกบิล+ทุกใบจ่าย**ของวันนั้นทั้งหมด ไม่เจาะจงรายการ

🔴 **`handle_payable_text()` คืน `False` เมื่อไม่ตรงคำสั่งไหนเลย และตัวเรียก (`handle_text`) ทิ้งค่านั้นไปเฉย ๆ
ไม่มี fallback message** — ในกลุ่ม `PAYABLE_GROUPS` คำสั่งที่พิมพ์ผิด/ไม่มีอยู่จริง = **เงียบสนิททุกครั้ง**
(ตระกูลเดียวกับ §3.8 กลุ่ม "เงียบ / พังโดยไม่มีใครรู้" — เจอเพิ่มอีกจุด)

### ⚠️ คำสั่งที่มีอยู่ก็ใช้แทนไม่ได้ตรง ๆ — เตือนไว้กันคนอื่นลองผิด
เคสจริง: วันที่ 2/9 มี 3 รายการ (18,795 · 5,445 · 19,236) ต้องการลบแค่ 18,795
- ❌ `ลบบิลล่าสุด` → ลบ **19,236** (รายการล่าสุดจริงของทั้งระบบ ไม่ใช่ 18,795)
  — `_delete_last_payable` ใช้ `ORDER BY id DESC LIMIT 1` ข้ามทุกวัน ไม่ใช่ "ล่าสุดของวันที่ต้องการ"
- ❌ `ลบวันที่ 2/9` เฉย ๆ → ลบทิ้งทั้ง 3 รายการ

### ทางแก้เฉพาะหน้า (ใช้คำสั่งเดิมที่มีอยู่ 3 บรรทัด — ปลอดภัย)
```
ลบวันที่ 2/9
บิล 2/9 5445
บิล 2/9 19236
```
ล้างทั้งวันแล้วใส่ 2 ใบที่ถูกกลับเข้าไป (คนละ id ใหม่ แต่วันที่/ยอดตรงเดิม ยอดค้างสะสมไม่กระทบ)
พิมพ์ `สรุปหนี้` เช็คหลังทำเสร็จ · ถ้าขึ้น **"คืนยอดที่จับคู่ไว้ไม่ครบ"** = มีใบใดใบหนึ่งถูกจ่ายไปแล้วบางส่วน ต้องดูเพิ่ม

### ทางแก้ถาวรที่ตกลงกันไว้ (รอเขียน)
1. เพิ่มคำสั่ง **`ลบบิล <วันที่> <ยอด>`** — ลบเจาะจงรายการเดียวโดยไม่ต้องอ้อม 3 ขั้น
   (เทียบทั้งวันที่**และ**ยอดให้ตรงเป๊ะ ก่อนลบ — เจอมากกว่า 1 ใบที่ตรงทั้งคู่ต้องเตือน ไม่ใช่ลบมั่ว)
2. **คำสั่งที่พิมพ์ไม่ตรงอะไรเลยในกลุ่มบัญชีหนี้ ต้องมีข้อความตอบกลับเสมอ** (อย่างน้อย "ไม่เข้าใจคำสั่ง")
   ไม่ใช่คืน `False` แล้วเงียบ — กติกาเดิมของโปรเจกต์ที่ยึดมาตลอด: "เงียบอันตรายกว่า error"

### ✅ แก้แล้ว — เพิ่มคำสั่งใหม่ + ปิดช่องเงียบ (merge เข้า `main` แล้ว `fd622db`)

1. **`ลบบิล <วันที่> <ยอด>`** — ลบเจาะจงรายการเดียว เทียบทั้งวันที่และยอดก่อนลบ
   (`ABS(amount-?)<0.01` กันเศษทศนิยม) · **ไม่มีช่องว่างหลัง "ลบบิล" ก็ใช้ได้** (พิมพ์แบบเจ้าของจริง)
   - ไม่พบใบตรงกัน → บอก "ไม่พบ" ไม่แตะข้อมูล
   - **พบมากกว่า 1 ใบที่วันที่+ยอดตรงกันพอดี → ปฏิเสธ ไม่เดา** (กติกาเดียวกับ `find_duplicate`)
     แนะนำให้ใช้ `ลบวันที่` แล้วพิมพ์ `บิล` ใส่ใบที่เหลือกลับแทน
   - ลบแล้วคืนยอดที่จับคู่ไว้ก่อนเสมอ (เหมือน `_delete_last_payable`/`ลบวันที่`)
2. **คำสั่งที่ขึ้นต้นด้วย "ลบ" แต่ไม่ตรงแพทเทิร์นไหนเลย → ตอบ "ไม่รู้จักคำสั่งนี้" พร้อมลิสต์คำสั่งที่ใช้ได้**
   แคบเฉพาะคำขึ้นต้น "ลบ" เท่านั้น — **ข้อความคุยทั่วไปที่ไม่ได้ขึ้นต้นด้วย "ลบ" ยังเงียบเหมือนเดิม**
   (เหตุผลเดียวกับบทเรียน "พี่ผมขอเคลียร์ยอดค้างเพิ่มให้หน่อยครับ" ที่เคยโดนตีความเป็นคำสั่งผิดมาแล้ว —
   ตอบแทรกทุกข้อความที่ไม่ตรงคำสั่ง = กวนกลุ่มที่คุยงานกันปกติ)

**เทสต์ใหม่ 12 เคส** (`tests/test_payable.py::TestDeleteBillByDateAmount` +
`::TestUnknownDeleteCommandNeverSilent`) — คุมครบ: ลบใบที่ระบุจริงไม่ใช่ใบ id สูงสุด ·
ยอดค้างลดลงพอดีเท่าที่ลบ · ไม่พบ = ไม่แตะข้อมูล · กำกวมหลายใบ = ปฏิเสธ · คืนยอดที่จับคู่ไว้ก่อนลบ ·
คำสั่งมั่วที่ขึ้นต้น "ลบ" ต้องได้ reply · คำสั่งเดิมที่ยังใช้ได้ต้องไม่โดน fallback แย่ง ·
แชทปกติที่ไม่ขึ้นต้นด้วย "ลบ" ต้องยังเงียบเหมือนเดิม
**พิสูจน์แล้วว่าตกจริง 10/12 เมื่อย้อนโค้ดกลับ** · รวมทั้งชุด **220 เคสเขียว**

⚠️ **เจอกับดัก isolation ระหว่างเขียนเทสต์:** ตัวกันสั่งซ้ำ (`_payable_del_key`) เก็บ timestamp ไว้ใน
`meta` ซึ่ง `PayableTestCase.setUp` ไม่ได้ล้าง (ล้างแค่ตาราง `payable_bills`/`payable_payments`) —
คลาสเทสต์เก่าที่เคยเรียก `ลบบิลล่าสุด` ทิ้ง timestamp ค้างไว้ ทำให้เทสต์ใหม่ที่รันทีหลังในไฟล์เดียวกัน
เจอการ์ด "ยืนยันซ้ำ" แทนการลบจริง (เขียวตอนรันไฟล์เดี่ยว ๆ แต่แดงตอนรันทั้ง suite) → ล้าง key นั้นเองใน
`setUp` ของคลาสใหม่ (บทเรียนเดียวกับ §3.9: ตั้งค่าที่ไม่ถูกล้างข้ามคลาสเทสต์ = เทสต์ผลลัพธ์ขึ้นกับลำดับรัน)

**ขอบเขตที่ตั้งใจไม่ทำ:** ยังไม่มี `ลบจ่าย <วันที่> <ยอด>` แบบเดียวกัน — เคสจริงที่เจอเป็นฝั่งบิลอย่างเดียว
เพิ่มให้ได้ถ้าเจอปัญหาเดียวกันฝั่งเงินจ่าย (เขียนแบบเดียวกันได้เลย ไม่ยาก)

### ✅ Deploy แล้ว — เจ้าของสั่ง "Deploy หมดเลย" (2 ก.ย. 18:55)
merge PR #140 เข้า `main` → merge commit `fd622db` → GitHub Actions run #65 `completed`/`success`
(ปกติกฎ "ห้าม push `main` ตอนร้านเปิด" ยังใช้อยู่เสมอ — รอบนี้ deploy เพราะเจ้าของสั่งเองตรง ๆ
ไม่ใช่ห้อง Code ตัดสินใจเอง)

### ⚠️ ระดับการตรวจ — ยังไม่เคยยืนยันบน production จริง
พิสูจน์ได้แค่ unit test บนเครื่อง dev (220 เคส) + จำลองข้อความจริงด้วยสคริปต์แยก (ผ่านครบ)
**ยังไม่เคยพิมพ์ `ลบบิล <วันที่> <ยอด>` ในกลุ่มดวงใจจริง** → เมื่อมีจังหวะ ลองสั่งจริงสักครั้งเพื่อยืนยัน

---

## 3.14 🩹 สลิปจ่ายดวงใจถูก reject ผิด (`PAYABLE_PAYEE_KEYWORDS` เป็นชื่อ ไม่ใช่เลขบัญชี) — 2-3 ก.ย. 2026

**เคสจริง 2 ก.ย. 23:46-23:47:** โอน 10,329 + 1,740 บาท (รวม 12,069) เข้าบัญชี "หจก. ดวงใจการสุรา"
ธ.กสิกรไทย xxx-x-x5342-x ถูกบอทตอบ **"⛔ สลิปนี้จ่ายเข้า '...' ไม่ใช่  → ไม่นับลดหนี้"** ทั้งที่จ่ายถูกบัญชีจริง

**ต้นเหตุ (ยืนยันด้วย `app.py:2868-2871`, `2788-2798`):**
`_payable_payee_ok()` เทียบ `info["receiver"]` ที่ AI อ่านได้กับ `PAYABLE_PAYEE_KEYWORDS` — สลิปคืนนั้น AI
อ่าน receiver ได้แค่ `"ธ.กสิกรไทย xxx-x-x5342-x"` (ไม่มีชื่อ "หจก. ดวงใจการสุรา" ติดมาด้วย) → เทียบ keyword
ไม่ผ่านเพราะ **ค่าจริงบน Render ตอนนั้นคือ `PAYABLE_PAYEE_KEYWORDS = "ดวงใจการสุรา, ดวงใจ"` (ชื่อล้วน)**
ทั้งที่ `HANDOFF.md` เขียนเตือนตัวเองไว้แล้วในตาราง §8 ว่าช่องนี้ควรใส่ "เลขบัญชี" เพราะ "OCR ชื่ออ่านเพี้ยนบ่อย"
— ค่าที่ตั้งไว้จริงไม่ตรงกับที่เอกสารแนะนำมาตั้งแต่ต้น **ไม่ใช่บั๊กครั้งเดียว เป็นช่องโหว่ที่เปิดค้างอยู่**
(เกิดซ้ำได้ทุกครั้งที่ AI ไม่อ่านชื่อติดมาด้วย)

**บั๊กที่สอง (พบพร้อมกัน):** `PAYABLE_VENDOR` บน Render **ว่างเปล่า** (ตั้งค่าไว้แต่เป็นสตริงว่าง ไม่ใช่ไม่ได้ตั้ง —
ดีฟอลต์ในโค้ด `os.environ.get("PAYABLE_VENDOR", "ดวงใจการสุรา")` ทำงานเฉพาะตอน **ไม่มีตัวแปรนี้เลย**)
กระทบข้อความที่มี `{PAYABLE_VENDOR}` แทรกอยู่ทุกจุด (สรุปหนี้รายวัน/รายสัปดาห์, ยอดค้างจ่ายสะสม, คู่มือบอท ฯลฯ
รวม ~10 จุดใน `app.py`) — ไม่ใช่บั๊กเงินผิด แต่ข้อความจะขึ้นช่องว่างลอยๆ แทนชื่อเจ้าหนี้ทุกจุด

### ✅ แก้แล้ว (env-only — ไม่ต้องแก้โค้ด/ไม่ต้อง deploy)
1. **เจ้าของแก้ `PAYABLE_PAYEE_KEYWORDS` บน Render แล้ว** เพิ่มเลขท้ายบัญชี `5342` เข้าไป
   → ค่าใหม่รวมทั้งชื่อเดิม + เลขบัญชี (ไม่ต้องทิ้งชื่อเดิม เผื่อ AI อ่านชื่อได้ปกติในสลิปอื่น)
2. **`PAYABLE_VENDOR`** — เจ้าของยืนยันว่าว่างเปล่าจริง (จากภาพหน้าจอ Render) — **ยังไม่ยืนยันว่าแก้ (ลบตัวแปร/
   ใส่ค่าคืน) เสร็จหรือยัง** ต้องเช็คอีกรอบ

### 🔎 ระดับการตรวจ — พิสูจน์ด้วยการยิงเข้าฟังก์ชันจริง `app._payable_payee_ok()` (ไม่ copy logic)
ตั้ง `PAYABLE_PAYEE_KEYWORDS="ดวงใจการสุรา, ดวงใจ, 5342"` แล้วเรียกฟังก์ชันจริงด้วย receiver จำลอง 5 แบบ:

| เคส | receiver | ผลลัพธ์ |
|---|---|---|
| สลิปเมื่อคืน (10,329) | `ธ.กสิกรไทย xxx-x-x5342-x` | ✅ `True` (เดิมจะเป็น `False`) |
| สลิปเมื่อคืน (1,740) | `ธ.กสิกรไทย xxx-x-x5342-x` | ✅ `True` |
| AI อ่านชื่อได้ครบตามปกติ | `หจก. ดวงใจการสุรา ธ.กสิกรไทย xxx-x-x5342-x` | ✅ `True` (ไม่กระทบเคสปกติ) |
| receiver อ่านไม่ได้เลย | `None` | ✅ `True` (ยังปล่อยผ่านเหมือนเดิม กันบล็อกสลิปจริง) |
| จ่ายเจ้าอื่นจริง (ต้องยังกันได้) | `ร้านเบียร์สด ธ.ไทยพาณิชย์ xxx-x-x9999-x` | ✅ `False` (ยังบล็อกถูกต้อง) |

**⚠️ ยังไม่ยืนยัน:** เลข `5342` ไม่ชนกับเลขบัญชีร้านเองใน `PAYEE_ACCOUNTS`/`PAYEE_KEYWORDS` (ห้องนี้มองค่านั้น
ไม่เห็น เป็นจุดไข่ปลาในภาพหน้าจอที่เจ้าของส่งมา) — เจ้าของยืนยันเองว่าไม่ชนแล้วก่อนหน้านี้ (บอกว่าเช็คให้)

### ✅ ปิดจบ 3 ก.ย. 00:39 น. — เจ้าของยืนยันเอง "เรียบร้อยหมดแล้ว"
- ยอดค้าง 12,069 (10,329 + 1,740) ที่เคย reject ผิด — แก้ด้วย `ลบจ่าย <วัน> <ยอด>` (PR #142)
  ล้างรายการ `จ่าย 10329` ที่พิมพ์ซ้ำออกแล้ว
- `PAYABLE_VENDOR` — เจ้าของยืนยันว่าเรียบร้อยเช่นกัน (ไม่ได้แจ้งรายละเอียดวิธีแก้มาที่ห้องนี้)

⚠️ **ระดับการตรวจที่เหลือ:** ยังไม่เคยเห็นสลิปจ่ายดวงใจจริงผ่านบอทจริงหลังแก้ env (`5342`ใน
`PAYABLE_PAYEE_KEYWORDS`) — ยืนยันแค่ระดับจำลองฟังก์ชัน (`app._payable_payee_ok()`) และคำยืนยัน
ด้วยวาจาจากเจ้าของว่า "เรียบร้อยหมดแล้ว" เท่านั้น ยังไม่มีหลักฐานเป็นข้อความจริงจากบอทที่ยืนยันว่าสลิป
ใหม่นับผ่านเองได้แล้ว — ถ้ามีจังหวะยืนยันด้วยสลิปจริงอีกใบจะปิดประเด็นนี้สนิท

---

## 3.15 🔀 สลับทางเข้า webhook มาที่ Render (Option B) — ทำจริงแล้ว 7 ก.ย. 2026 ✅

**ค้างมาตั้งแต่ 20 ส.ค.** (`TEAM.md` กล่องข้อความ) รอบนี้เจ้าของสั่งทำเลย ไม่รอนัดคืนแล้ว

### เส้นทาง event เปลี่ยนเป็น
```
เดิม:  LINE → line_bridge (Apps Script ของห้องคอนเทนต์) → Render (บอทร้าน)
ใหม่:  LINE → Render (บอทร้าน) → forward ต่อ → line_bridge (คอนเทนต์)
```
**ทำไมดีกว่า:** เดิมบอทร้านฝากชีวิตไว้กับ Apps Script ของอีกห้อง (deploy พลาด/cold start/โควตาหมด = ร้านดับ)
ตอนนี้ Render เป็นทางเข้า (อุ่นเครื่องตลอด ไม่มี cold start) ใครล่มก็ล่มเฉพาะฝั่งนั้น

### ทำอะไรไป
- เจ้าของเปลี่ยน Webhook URL ใน LINE Developers → ชี้ `https://line-slip-bot-1-iq8e.onrender.com/callback`
  (ทำไปก่อนหน้าแล้ว ทำให้ฝั่งคอนเทนต์เงียบไปหลายวันจนกว่าจะตั้ง env ข้างล่าง)
- ตั้ง env `WEBHOOK_FORWARD_URLS` ที่ Render (7 ก.ย. ~15:53 น.) = URL ของ `line_bridge` + `?src=render`
  **โค้ดฝั่ง Render มีมาตั้งแต่ 19 ส.ค. แล้ว ปิดอยู่เพราะ env ว่าง** — รอบนี้แค่ตั้ง env ไม่ต้อง deploy โค้ด
- ฝั่งคอนเทนต์ตั้ง `STANDALONE = true` (เลิก forward กลับ) + ใช้ `?src=render` กันลูป 2 ชั้น

### ✅ ระดับการตรวจ — ยืนยันบน production จริงแล้ว (ครั้งแรกของงานชุดนี้)
เจ้าของทดสอบเองครบ 4 ข้อ **ผ่านหมด**: `@bot /ช่วย` ตอบ · รูปเซฟเข้า Drive · จองโต๊ะยังทำงาน · ไม่มี event ซ้ำ
หลักฐานจาก Render log: `POST /callback 200` จาก `LineBotWebhook/2.0` (LINE ยิงตรงจริง) · ไม่มี `[forward]` error
(โค้ด log เฉพาะตอน forward ล้มเหลว → เงียบ = ส่งต่อสำเร็จ) · `[skip] message_id ซ้ำ` = LINE redelivery
ที่ตัวกันซ้ำจับได้ **ไม่ใช่ loop**

### 🪤 กับดักที่เกือบโดน (บันทึกไว้)
URL ของ Apps Script ที่แจ้งกันรอบก่อน **ตัวที่ 7 เป็นเลข `2` ซึ่งผิด** ตัวจริงเป็น `z` ตัวเล็ก
(`AKfycbz4...` ไม่ใช่ `AKfycb24...`) — ผิดตัวเดียวเสียเวลาไล่กันทั้งวัน · **URL แบบนี้ต้องก๊อป ห้ามพิมพ์ตาม**

---

## 3.16 🫀 สัญญาณชีพคอนเทนต์ต่อท้ายรายงาน 00:30 — เขียนเสร็จ 7 ก.ย. (PR #143 ยังไม่ merge)

**ปัญหาที่แก้:** ระบบคอนเทนต์เคย **ตายเงียบ 7 วัน** โดยไม่มีอะไรฟ้อง และรอบ 5 ก.ย. ก็เงียบอีกหลายวัน
— เพราะ "ไม่มีข้อความออกมา" หน้าตาเหมือน "วันนี้ไม่มีอะไรเกิดขึ้น" เป๊ะ

**ทางแก้ที่ตกลงกับห้องคอนเทนต์ไว้ตั้งแต่ 19 ส.ค.:** ให้ **บอท** เป็นคนรายงาน ไม่ใช่ bridge
(bridge คือตัวที่มีโอกาสพังที่สุด ถ้าให้ตัวที่พังเป็นคนรายงาน วันพังจริงจะไม่มีรายงานออกมา)
ฝั่งคอนเทนต์ทำ endpoint คืน JSON เสร็จแล้ว บอทดึงมาต่อท้ายรายงาน 00:30 ที่ยิงอยู่แล้วทุกคืน

**env 3 ตัว** (ไม่ตั้ง = ปิดสนิท ไม่กระทบรายงานเดิมสักกลุ่ม):
`CONTENT_HEARTBEAT_URL` (URL+รหัสลับ) · `CONTENT_HEARTBEAT_GROUP` (กลุ่มเดียวที่จะให้บรรทัดนี้โผล่ —
ไม่งั้นทุกกลุ่มรับสลิปจะเห็นตัวเลขคอนเทนต์ที่ไม่เกี่ยวกับตัวเอง) · `CONTENT_HEARTBEAT_TIMEOUT` (ดีฟอลต์ 8 วิ)

**ธงที่ตั้งใจให้ดัง:** `failed`>0 → 🚨 แยกบรรทัด · วันที่ในข้อมูลไม่ตรงวันรายงาน → เตือน "bridge อาจค้าง" ·
โชว์ชื่อโมเดล AI ทุกวัน (ผู้ให้บริการปลดโมเดลเก่าจะเห็นก่อนพัง) · `holiday` โชว์กำกับ `planned=0`

⚖️ **กติกาที่คุมด้วยเทสต์:** ดึงตัวเลขไม่ได้ **ต้องไม่ทำให้รายงานสลิปหายทั้งฉบับ** — timeout/HTTP error/
JSON เสีย/JSON ไม่ใช่ object ทุกกรณีขึ้น `⚠️ ดึงตัวเลขไม่ได้` แล้วรายงานสลิปยังครบ
(เครื่องมือที่ทำไว้กันเงียบ ห้ามกลายเป็นจุดพังใหม่เสียเอง)

### 🔴 ยังไม่เสร็จ
- **PR #143 ยังไม่ merge** → โค้ดยังไม่ขึ้น production · ตั้ง env ไปก่อนก็ยังไม่มีผล (บอทตัวที่รันอยู่ไม่รู้จักชื่อ env)
- **ยังไม่ได้ URL endpoint ตัวจริงจากห้องคอนเทนต์** (มีรหัสลับ ส่งให้เจ้าของโดยตรง ไม่ผ่านกล่องกลาง)
- ระดับการตรวจ: เทสต์ 11 เคสด้วย response จำลอง (241/241 เขียว) — **ยังไม่เคยยิง endpoint จริง**
  ต้องดูรายงาน 00:30 คืนแรกหลัง merge+ตั้ง env ว่าตัวเลขตรงกับที่ endpoint คืนไหม

---

## 3.17 🩹 จองหายเงียบเพราะ AI ตอบ JSON ไม่จบ — 8 ก.ย. 2026 ✅ merge + deploy แล้ว (run #73)

พนักงานพิมพ์ `รับจองคุณโอม 5 ท่านโต๊ะ B10  1 ทุ่ม ครับ @All` → **บอทเงียบสนิท** จองหลุดโดยไม่มีใครรู้

**ต้นเหตุ (จาก Render log ไม่ใช่การเดา):** AI ตอบ `is_reservation:true` พร้อมข้อมูลครบ
แต่ JSON **ถูกตัดกลางคัน ไม่มี `}` ปิด** → `json.loads` พัง → ตัวสำรอง regex `\{.*\}` ก็หาไม่เจอ
→ ตกไป `fallback` ซึ่งแปลว่า "ไม่ใช่การจอง" → ข้ามเงียบ

⚠️ **จุดที่ออกแบบพลาด:** `fallback` ก้อนเดียวถูกใช้แทนสองความหมายที่ต่างกันสิ้นเชิง —
**"AI ว่าไม่ใช่จอง"** (เงียบถูกแล้ว) กับ **"AI ตอบไม่ได้"** (ห้ามเงียบเด็ดขาด)

**แก้ 3 ชั้น (PR #144):**
1. `_repair_truncated_json()` — ปิดวงเล็บให้ก่อน (ไม่เสียคู่สุดท้ายที่สมบูรณ์อยู่แล้ว) ถ้ายังไม่ได้
   ค่อยตัดคู่ท้ายที่ไม่สมบูรณ์ทิ้งทีละคู่ · **ไม่เดาค่าที่ขาด** — ไม่รู้ = ไม่มีฟิลด์
2. ซ่อมไม่ขึ้น → **ขอใหม่ 1 รอบด้วย `GEMINI_MODEL_RETRY`** · ของเดิม `_gemini_generate` retry
   เฉพาะตอน **เรียกไม่ติด** (exception) ส่วนคำตอบที่ตอบมาครบแต่เนื้อในใช้ไม่ได้ **ไม่เคยถูกลองซ้ำเลย**
3. ธง `_unreadable` แยก "อ่านไม่ออก" ออกจาก "ไม่ใช่จอง" → ตอบ `⚠️ อ่านข้อความจองนี้ไม่ออก` แทนการเงียบ

**+ `_gemini_finish_reason()`** — log ว่าโมเดลหยุดเพราะ `MAX_TOKENS`/`STOP`/`SAFETY`
ของเดิม log บอกแค่ "parse ไม่ได้" จึงเหลือแต่การเดาว่าชนลิมิตหรือสะดุด ซึ่งแก้คนละวิธี

### สมมติฐาน "คำว่า รับจอง มีปัญหา" — ทดสอบแล้ว ไม่จริง
เจ้าของสงสัยว่าเป็นที่คำว่า `รับจอง` (ขณะที่ `จอง` เฉยๆ ไม่มีปัญหา) ทดสอบบน production 8 ก.ย. 18:32-18:33
(หลัง run #73 เสร็จ 18:18): `จอง...` → จอง #74 · `รับจอง...` → จอง #75 · `รับจอง...B10 1 ทุ่ม @All`
(รูปแบบเดียวกับเคสคุณโอมเป๊ะ) → ผ่าน · **บอทตอบครบทั้ง 3** และ **ไม่มีบรรทัด `ถูกตัดกลางคัน` เลย**

โค้ดยืนยันอยู่แล้วว่า `รับจอง` กับ `จอง` เดินเส้นทางเดียวกันเป๊ะ (`_RESV_HINTS` เช็คแบบ substring ·
prompt เหมือนกันทุกตัวอักษร) 📌 **อาการนี้เป็น transient ไม่ผูกกับคำ**

### 🔴 ค้างไว้
- **ยังไม่รู้ว่าทำไม Gemini ตอบไม่จบ** — `finish_reason` ขึ้น production แล้ว ครั้งหน้าที่เกิด log จะบอกเอง
  **ห้ามแก้อะไรเพิ่มจนกว่าจะเห็นค่านั้น** — แก้ไปก่อนคือเดา
- ยืนยันบน production 3/3 แต่ **ยังไม่เคยเห็นชั้นซ่อม/ชั้นขอใหม่ทำงานจริง** เพราะรอบทดสอบ JSON ไม่ขาด

---

## 3.18 🩹 สลิปจ่ายเจ้าหนี้ถูกอ่าน "สลับด้าน" → ปฏิเสธด้วยเหตุผลที่ผิด — 9 ก.ย. 2026 (PR รอ merge)

### เกิดอะไรขึ้น
9 ก.ย. 00:00 น. สลิป K+ กสิกร→กสิกร 10,000 บาท (บันทึกช่วยจำ: `ไส้ (28/8) จ่ายที่ค้างจบ`):
```
ผู้โอน  นาง พิมนภัทร์ ส        ธ.กสิกรไทย xxx-x-x4612-x
  ↓
ผู้รับ  หจก. ดวงใจการสุรา      ธ.กสิกรไทย xxx-x-x5342-x
```
บอทตอบ: `⛔ สลิปนี้จ่ายเข้า 'นาง พิมลักษณ์ ส. ธ.กสิกรไทย xxx-x-x4612-x' ไม่ใช่ ดวงใจการสุรา`

**AI หยิบ "ฝั่งผู้โอน" มาเป็น `receiver`** → ด่านปลายทางปฏิเสธ ทั้งที่จ่ายเจ้าหนี้จริง
เจ้าของต้องมาพิมพ์ `จ่าย <ยอด>` เองทุกใบ

### ต้นเหตุ — บทเรียนเก่าไม่ได้ถูกยกมาใช้
🪤 **กฎกัน "อ่านสลับด้าน" ถูกเขียนไว้ในพรอมป์ตสลิปรายรับตั้งแต่เคส 19/08/26** (บนลงล่าง · ลูกศร ·
ยึดคำว่า จาก/ไปยัง · รูปหมุน · แถบแจ้งเตือน=ผู้โอน) **แต่ไม่เคยถูกยกมาที่พรอมป์ต `extract_payable_doc`**
ซึ่งบอกแค่ *"receiver = บรรทัด 'ไปยัง/ผู้รับ/โอนเข้า'"* เฉยๆ → บั๊กเดิมกลับมาเกิดที่เส้นทางที่สอง

เส้นสลิปรายรับยังมี **ตัวจับสลับด้าน** อยู่แล้วด้วย (`app.py` ~5311 — เจอชื่อร้านฝั่งผู้โอน =
ติดป้าย "⚠️ อาจอ่านสลับด้าน") **เส้นเจ้าหนี้ไม่มี**

### แก้ 3 จุด
1. **ยกกฎ 🧭 ทั้งชุดมาไว้ในพรอมป์ตเจ้าหนี้** + เพิ่มข้อใหม่ *"สองฝั่งเป็นธนาคารเดียวกันเป็นเรื่องปกติ
   ห้ามใช้โลโก้ตัดสินว่าใครเป็นผู้รับ"* (เคสนี้กสิกร→กสิกร โลโก้เหมือนกันทั้งคู่)
2. **`_payable_recheck_payee()`** — ปลายทางไม่ตรง = จุดที่พลาดแล้วเสียเงินจริง → **อ่านซ้ำด้วย pro
   ก่อนปฏิเสธ** · ของเดิม pro-retry ยิงเฉพาะตอน "อ่านไม่ออก/ยอด≤0" ไม่เคยยิงตอนนี้ ทั้งที่แพงกว่า ·
   รับผลรอบสองเฉพาะเมื่อ *เป็นสลิป + ยอด>0 + ปลายทางตรงเจ้าหนี้* ไม่งั้นคืนของเดิม (กันยอดเพี้ยน)
3. **`_payable_payee_on_sender()`** — เจอบัญชีเจ้าหนี้อยู่ฝั่งผู้โอน → ข้อความใหม่ที่บอกความจริง
   แทนเหตุผลที่ผิด พร้อมยอดเติมมาให้เลย:
   ```
   ⚠️ สลิปนี้ยังไม่นับลดหนี้ — บอทอ่านว่าปลายทางคือ 'X' แต่เห็นบัญชี ดวงใจการสุรา อยู่ 'ฝั่งผู้โอน'
   เป็นได้ 2 อย่าง บอทแยกเองไม่ได้:
     1) บอทอ่านสลับด้าน (จ่ายดวงใจจริง) → พิมพ์ 'จ่าย 10,000.00' เพื่อบันทึก
     2) ดวงใจการสุรา โอนคืนร้าน → ไม่ต้องทำอะไร
   ```

⚖️ **ตั้งใจไม่กลับข้างให้เอง** — สลิป "ดวงใจโอนเงินคืนร้าน" หน้าตาแบบนี้เป๊ะเหมือนกัน (sender=ดวงใจ จริงๆ)
แยกจากกันไม่ได้ด้วยข้อมูลที่มี · เรื่องเงินมีมากกว่าหนึ่งความเป็นไปได้ = **ให้คนตัดสิน**
(ยึดแนวเดียวกับด่านสลิปรายรับ)

### 🪤 ของแถมที่เจอตอนไล่เคสนี้ — เส้นทางรูปเจ้าหนี้ "เงียบใน log"
เจ้าของค้นคำว่า `payable` ใน Render log ช่วงเวลาที่เกิดเหตุ → **ไม่เจออะไรเลยสักบรรทัด**

ไม่ใช่เพราะหาผิดที่ — **ขาปฏิเสธไม่ print อะไรเลยจริงๆ** มันเรียกแค่ `record_image_miss()`
(เขียน DB เงียบๆ) + `notify()` (ตอบในไลน์) · ขาบันทึกสำเร็จก็เงียบเหมือนกัน
ของเดิม log เฉพาะตอน **พัง** (โหลดรูปไม่ได้ / ไม่ใช่บิลสลิป / pro-retry)
→ **ขาที่ทำให้เงินหายจากบัญชีหนี้ กลับเป็นขาที่มองไม่เห็นที่สุด**

เพิ่ม log 4 จุด: `อ่านได้: type/amt/date/receiver/sender/ref` (บรรทัดที่คืนนั้นต้องการแต่ไม่มี) ·
`⛔ ปฏิเสธ: ปลายทางไม่ตรงเจ้าหนี้` · `✅ บันทึกบิล` · `✅ บันทึกจ่าย`
เทสต์ใหม่ 5 เคสรันผ่าน `_process_payable_image` ตัวจริง (เป็นเทสต์ end-to-end ชุดแรกของขาปฏิเสธ)

### 🔴 ค้างไว้ / ระดับการตรวจ
- เทสต์ใหม่ **21 เคส** (278/278 เขียว) — คุมสองทิศ: จ่ายเจ้าอื่นต้องยังถูกปฏิเสธ · จ่ายเจ้าหนี้จริง
  ที่อ่านสลับต้องไม่ถูกปฏิเสธด้วยเหตุผลผิด
- **ยังไม่ยืนยันบน production** — ต้องส่งสลิปใบเดิมซ้ำหลัง deploy แล้วดูว่าผ่านด่านไหม
- **ยังไม่มี log ของเคสนี้** — ยังไม่รู้แน่ว่า AI คืน `receiver` มาเป็นอะไรเป๊ะๆ
  (ปฏิเสธด้วยข้อความที่มี `xxx-x-x4612-x` = ฝั่งผู้โอน ซึ่งเป็นหลักฐานพอให้แก้ แต่ไม่ใช่ตัว JSON จริง)
- **10,000 บาทใบนี้ยังไม่เข้าระบบ** ถ้ายังไม่ได้พิมพ์ `จ่าย 10000` ต้องพิมพ์เอง

---

---

## 3.19 🩹 สลิปลงวันผิด 2 เดือน — AI อ่าน "ก.ย." เป็น "ก.ค." — 13 ก.ย. 2026 (PR รอ merge)

### เกิดอะไรขึ้น
13 ก.ย. 00:13 น. สลิป K+ โอน 10,000 เข้าดวงใจ · บนสลิปเขียน `13 ก.ย. 69` ·
บันทึกช่วยจำ: `ไส้ (5/9) ค้าง 12,667`

สรุปหนี้ที่บอทเด้งกลับมามีบรรทัด:
```
13/07/26  💰 −10,000.00      ← ควรเป็น 13/09/26
05/09/26  📥 +22,667.00
```
**เงินจ่ายไปโผล่ก่อนบิลที่มันจ่ายเกือบ 2 เดือน**

### ต้นเหตุ
**`ก.ย.` (=09) กับ `ก.ค.` (=07) ต่างกันตัวเดียว** — พรอมป์ตบอกแค่ *"พ.ศ. ลบ 543"*
ไม่เคยบอกว่าตัวย่อเดือนไทยตัวไหนคือเลขอะไร และคู่ไหนสลับกันบ่อย

🪤 **ด่าน `_sane_doc_date()` ปล่อยผ่าน** — มันกันแค่ "อนาคต" กับ "เกิน `PAYABLE_DATE_MAX_DAYS` (90 วัน)"
ส่วน 13/07 ห่างจากวันนี้ **62 วัน** ยังไม่เกิน จึงดูสมเหตุผลในสายตาโค้ด
(ด่านนี้ถูกออกแบบมากันคนละเรื่อง — กัน AI หยิบเลขที่บิล/วันครบกำหนดมาเป็นวันที่)

### แก้ 2 ชั้น
1. **ตารางเดือนไทยในพรอมป์ต** — `ม.ค.=01 … ธ.ค.=12` พร้อมเตือนคู่ที่ต่างกันตัวเดียว:
   **ก.ย.(09)↔ก.ค.(07)** · **พ.ค.(05)↔พ.ย.(11)** · **มี.ค.(03)↔มิ.ย.(06)** · ม.ค.(01)↔มี.ค.(03)
   ⚖️ **ใส่ทั้งพรอมป์ตเจ้าหนี้และพรอมป์ตสลิปรายรับพร้อมกัน** — กับดักเดียวกันเป๊ะ
   (บทเรียนตรงจาก §3.18 ที่แก้ทางเดียวแล้วบั๊กเดิมไปโผล่อีกทาง)
2. **`_payment_date_contradicts()`** — วันโอน **ย้อนไปก่อนบิลที่มันจ่าย** (ตามโน้ตบนสลิป) = เป็นไปไม่ได้
   → ทิ้งวันที่นั้น ใช้ "วันที่ส่ง" แทน **แล้วบอกในแชทว่าเปลี่ยนให้เพราะอะไร** (ห้ามแก้เงียบ)
   ⚠️ **ไม่เดาว่าเดือนที่ถูกคืออะไร** — แค่ตอบว่าค่านี้ขัดแย้งในตัวเอง ใช้ไม่ได้
   ⚠️ **ไม่มีโน้ตบอกบิล = ไม่ตัดสิน** (เจ้าของส่งสลิปย้อนหลังเก็บตกได้จริง — ห้ามทิ้งวันที่ที่อาจถูกอยู่)

### ระดับการตรวจ
- เทสต์ใหม่ **10 เคส** (288/288 เขียว) — รวม end-to-end ผ่าน `_process_payable_image` ตัวจริง
  คุมสองทิศ: เดือนเพี้ยนต้องลงวันนี้ + บอกในแชท · **วันที่ถูกอยู่แล้วห้ามไปยุ่ง** ·
  แก้วันที่ต้องไม่กระทบการตัดยอดตามโน้ต
- ⚠️ **ยังไม่ยืนยันบน production**
- 🔴 **แถวผิดในบัญชีจริงยังอยู่** — ต้องลบ `13/07/26 −10,000` แล้วส่งสลิปใหม่หลัง deploy

---

---

## 3.20 💳 รายงานยอดขายซ่อนช่องทางรับเงิน — "ค้างชำระ" หายเงียบ — 13 ก.ย. 2026 (PR รอ merge + **paste มือ**)

### เจอได้ยังไง
เจ้าของส่งรายงาน 12/09/26 มาให้ดู — บวกเลขตามแล้วไม่ลงตัว:
```
💰 ยอดขาย: ฿88,823
💳 โอน 73,292 · สด 4,224 · บัตร 8,590     รวม = 86,106   → ขาด 2,717
```
(ส่วนอื่นตรงหมด: 88,823 − 71,263 = 17,560 ✓ · 88,823 ÷ 81 = 1,097 ✓)

### ต้นเหตุ — บรรทัดเดียว
`buildDailyMsg_()` พิมพ์ช่องทางรับเงินแบบ **ฮาร์ดโค้ด 3 ช่อง**:
```js
'💳 โอน '+fmtT_(pm['เงินโอน']||0)+' · สด '+fmtT_(pm['เงินสด']||0)+' · บัตร '+fmtT_(pm['บัตรเครดิต']||0)
```
แต่ `PAY_MAP` (บรรทัด 325) แกะออกมา **5 ช่อง**:
```js
{'Prompay':'เงินโอน','เงินสด':'เงินสด','บัตรเครดิต':'บัตรเครดิต','ค้างชำระ':'ค้างชำระ','อื่นๆ':'อื่นๆ'}
```
→ **`ค้างชำระ` กับ `อื่นๆ` ถูกทิ้งเงียบทุกวัน**

🪤 ระบบ *รู้อยู่แล้ว* ว่ามีคีย์พวกนี้ — `parseBalance_()` ถึงกับมีคอมเมนต์ว่า
*"'อื่นๆ' โผล่ 2 แถว (1,660 + ทชท 1,683 = 3,343) ถ้าทับจะหายไปแถวหนึ่ง"*
คนเขียนตัวแกะใส่ใจมาก แต่ตัวที่เอาไปแสดงผลกลับตัดทิ้ง

⚖️ **`ค้างชำระ` = เงินที่ยังเก็บไม่ได้** — ตัวเลขที่เจ้าของต้องเห็นที่สุด กลับเป็นตัวที่ถูกซ่อน

### แก้ 3 จุด (`dashboard/Code_full.gs`)
1. **`_payLine_(pm)`** — โชว์ 3 ช่องหลักเสมอ (0 ก็โชว์) + **ทุกช่องที่มียอดจริง** ต่อท้าย
2. **เตือน `ค้างชำระ` แยกบรรทัด** ใน "ข้อควรระวัง" — ยังเก็บเงินไม่ได้ ต้องตามเก็บ
3. **เตือนเมื่อเงินรับรวม ≠ ยอดขาย** เกิน 1% หรือ 200 บาท — ถ้า POS มีช่องทางใหม่ที่ `PAY_MAP`
   ยังไม่รู้จัก จะไม่หายเงียบอีก (ตัวจับรุ่นก่อนหน้าก็มี — `checkSaleTotals()` — แต่ต้องสั่งรันเอง
   และเทียบคนละคู่ ไม่ได้เห็นในรายงานรายวัน)

### ระดับการตรวจ
รันตรรกะจริงด้วย node กับตัวเลขของ 12/09 (สมมติส่วนที่หายเป็น ค้างชำระ 2,000 + อื่นๆ 717):
```
💳 โอน 73,292 · สด 4,224 · บัตร 8,590 · ค้างชำระ 2,000 · อื่นๆ 717
รวมเงินรับ = 88,823 | ยอดขาย = 88,823 | ต่าง = 0
```
เช็คกรณีว่าง/ช่องเดียวแล้ว ไม่มี `undefined`/`NaN` โผล่

⚠️ **ยังไม่รู้ว่า 2,717 จริงๆ อยู่ช่องไหน** — ตัวเลขข้างบนเป็นค่าสมมติเพื่อทดสอบตรรกะเท่านั้น
จะรู้ของจริงก็ต่อเมื่อ paste โค้ดใหม่แล้วรายงานวันถัดไปออกมา

### 🔴 ต้อง paste มือ — merge อย่างเดียวไม่พอ
`dashboard/Code_full.gs` เป็น**สำเนาอ้างอิงในรีโป** ไม่มี clasp/CI ผูกกับ Apps Script จริง (ดู §3.12)
→ ต้องเปิด Apps Script แล้ววางทับทั้งไฟล์เอง · ขั้นตอนคลิกต่อคลิกอยู่ที่
`dashboard/DEPLOY_STEPS_2026-09-02.md`

📌 **paste ครั้งเดียวได้ 2 อย่าง** — ของ §3.12 (ค่าใช้จ่ายนับ 7 รอบ) ยังค้างไม่เคย paste เหมือนกัน

---

---

## 3.21 🔎 ตัวตรวจข้อมูลตรวจแค่เดือนล่าสุด — พฤษภาคมหายทั้งเดือนโดยไม่มีใครรู้ — 14 ก.ย. 2026 ✅ **ปิดเคส (ยืนยัน production)**

### เจอได้ยังไง
เจ้าของ paste §3.20 เสร็จแล้วรัน `debugDaily()` เพื่อยืนยันว่าโค้ดใหม่ทำงาน — โค้ดทำงานปกติ
แต่ **ผลที่พิมพ์ออกมาเผยของที่ไม่มีใครเคยเห็น**:
```
📊 saisang_data.json มี 8 เดือน
   • มกราคม · กุมภาพันธ์ · มีนาคม · เมษายน · [ไม่มีพฤษภาคม] · มิถุนายน · กรกฎาคม · สิงหาคม · กันยายน
```

### 🪤 ช่องโหว่ 2 อย่างของตัวตรวจเอง
1. **เช็ค "วันที่ขาด" เฉพาะเดือนล่าสุดเดือนเดียว** — บรรทัด `เดือนล่าสุด 2026-09: ขาด 0 วัน ✅`
   เดือนก่อนหน้าไม่เคยถูกตรวจเลย · เห็น `กรกฎาคม = 29 วัน` (ควรมี 31) ก็บอกไม่ได้ว่าขาดวันไหน หรือขาดจริงไหม
2. **พิมพ์เฉพาะเดือนที่ "มี"** — เดือนที่หายไปทั้งเดือนจึงไม่มีบรรทัด = **มองไม่เห็น**
   ต้องนั่งนับชื่อเดือนเองถึงจะรู้ว่าพฤษภาคมหาย

### สรุปข้อเท็จจริงหลังตรวจ
| เดือน | สถานะ |
|---|---|
| **พฤษภาคม 2569** | 🔴 **หายทั้งเดือน** · เจ้าของยืนยัน **ร้านเปิดปกติ** = ข้อมูลหายจริง (ยอดรวมทั้งปีขาด ~2.2 ล้าน) |
| กรกฎาคม 29/31 วัน | ✅ **ครบ** — 29-30 ก.ค. ร้านหยุด และอยู่ใน `CLOSED_DAYS` อยู่แล้ว (เจ้าของยืนยัน) |
| ม.ค.–มิ.ย. ไม่มี `daily_totals` | ℹ️ เดือนเก่าที่นำเข้าก่อนมีฟิลด์นี้ — ยอดรวมรายเดือนใช้ได้ แต่กราฟรายวันว่าง |

### แก้ (`dashboard/Code_full.gs` · `debugDaily()`)
- **ตรวจวันที่ขาดทุกเดือนที่มีข้อมูล** ไม่ใช่เดือนล่าสุดเดือนเดียว · ยังหักวันร้านหยุดตาม `CLOSED_DAYS`
  และพิมพ์กำกับว่าหักไปกี่วันวันไหน (`ครบเพราะร้านหยุด` ต้องอ่านออก ไม่ใช่เดา)
- **เดือนแรกสุดเริ่มนับจากวันแรกที่มีข้อมูล** — เก็บกลางเดือนไม่ใช่ "ขาด" (กันเตือนหลอก)
- **`_ymOfPeriod_()` + ตัวไล่ช่องว่างเดือน** — เดือนที่หายทั้งเดือนต้องมีบรรทัด 🔴 บอกออกมาตรงๆ

### ระดับการตรวจ
รัน `debugDaily()` ตัวจริงด้วย node กับข้อมูลจำลองที่ **สร้างให้ตรงกับผลรันจริงของเจ้าของเป๊ะ**
(ก.ค. 29 วัน + หยุด 29-30 · ส.ค. 31 · ก.ย. 13 · saisang 8 เดือนไม่มีพฤษภาคม) — ได้ผลตามคาด:
```
• 2026-07: มี 29 วัน · ขาด 0 วัน ✅ ครบ  (ไม่นับวันร้านหยุด 2 วัน: 2026-07-29, 2026-07-30)
🔴 เดือนที่หายไปทั้งเดือน (1): 2026-05
```
syntax ทั้งไฟล์ผ่าน `node --check`

### 🪤 ตัวกู้เองก็กู้พฤษภาคมไม่ได้ — ค้นเมลย้อนได้แค่ 90 วัน (แก้ในรอบเดียวกัน)
เจ้าของยืนยัน **"เมลยังอยู่ ไปกู้มาได้เลย"** — แต่ก่อนสั่งรัน ตรวจโค้ดพบว่ารันไปก็ไม่เจอ:

`_findPosMsg_()` (ตัวค้นเมลที่ `importPosByDate` / `debugSaleAround` ใช้ร่วมกัน) ค้นด้วย
```js
GmailApp.search('has:attachment filename:pdf newer_than:90d', 0, 150)
```
มี **2 เพดานที่ทำให้กู้เดือนเก่าไม่ได้เลย**:
1. **`newer_than:90d`** — วันนี้ 14 ก.ย. ย้อนได้ถึง ~16 มิ.ย. → **พฤษภาคมอยู่นอกกรอบทั้งเดือน**
   รันไปจะขึ้น `❌ ไม่พบเมลรายงาน POS` ทั้งที่เมลอยู่ครบ = **อ่านแล้วเข้าใจผิดว่าเมลหาย**
2. **`,0,150`** — ตัดที่ 150 เธรด และ Gmail คืน **ใหม่สุดก่อน** → เดือนเก่าสุด (ตัวที่กำลังจะกู้พอดี)
   คือตัวที่ถูกตัดทิ้งเงียบๆ · ยิ่งกู้ย้อนไกล ยิ่งไม่มีทางเจอ

**แก้:** ค้นจาก **"วันที่ขอกู้"** แทน **"กี่วันย้อนหลังจากวันนี้"**
- `_gmailWindow_(isos)` → `after:/before:` คร่อมช่วงที่ขอ (เผื่อหน้า 3 วัน หลัง 5 วัน เพราะเมลของวัน D
  เข้ามาวัน D+1 และคน export ย้อนหลังทีหลังได้) · ผลพลอยได้คือค้นแคบลงมาก เร็วขึ้นด้วย
- **ไล่ทีละหน้าจนหมด** (100/หน้า เพดาน 2,000 + เตือนถ้าชน) ไม่ตัดกลางคัน
- `importPosByDate` ส่ง `DATES` ทั้งชุดให้ตัวค้น → **สแกนเมลครั้งเดียวต่อรอบ** ไม่ใช่ต่อวัน
- **`monthDates_('2026-05')`** — สร้างลิสต์ทั้งเดือนให้ ไม่ต้องพิมพ์ 31 บรรทัดเอง · หักวันใน `CLOSED_DAYS` ออกให้

ตรวจด้วย node: `monthDates_('2026-05')` → 31 วัน · query ที่ยิงจริง =
`has:attachment filename:pdf after:2026/04/28 before:2026/06/05` ✅ ครอบพฤษภาคมครบ
(`monthDates_('2026-07')` → 29 วัน หัก 29-30 ที่ร้านหยุดออกให้แล้ว)

### ✅ ทางที่เลือก — กู้ "ยอดรวมเดือน" ใบเดียว ไม่ต้องกู้รายวัน 31 วัน

สังเกตจากผลรัน: **ม.ค. · ก.พ. · มี.ค. · เม.ย. · มิ.ย. ไม่มี `daily_totals` อยู่แล้วทุกเดือน**
→ พฤษภาคมต้องการแค่ **ยอดรวมเดือนตัวเดียว** ก็เท่ากับเพื่อนบ้านมันแล้ว ยอดรวมทั้งปีหายเพี้ยนทันที
(รายวัน 31 วัน × ~4 OCR = ~124 ครั้ง ชนโควตาหลายรอบ กินเวลาทั้งวัน · เจ้าของยืนยัน POS export
ช่วงวันที่ได้ จึงเลือกทางนี้)

**`importMonthTotal()`** — ใหม่:
- หา SaleReport ใบใหม่สุดในเมล 30 วัน (ใบเพิ่ง export) เปิดทีละใบ **มากสุด 5 ใบ** คุมโควตา OCR
- **ช่วงวันที่บนใบต้องตรง 1–สิ้นเดือน เป๊ะ** ไม่ตรง = ไม่บันทึก (บทเรียน 19/08/26 ที่เชื่อหัวเรื่องแล้วทับผิดวัน)
- ใบจ่ายเงินนับเป็นค่าใช้จ่ายเฉพาะเมื่อ **ครอบช่วงเดียวกันเป๊ะ** · ไม่มี = `total_expenses` ไม่มีฟิลด์
  ⚖️ **ไม่รู้ ≠ 0** ห้ามใส่ 0 ให้กราฟอ่านว่ากำไรเต็ม
- **จงใจไม่ใส่ `daily_totals`/`payment_days`** — ใบสรุปเดือนไม่มีข้อมูลรายวัน ใส่ค่าปลอมแย่กว่าไม่มี
- **ไม่แตะ `pos_daily.json`** เลย

⚠️ **ห้ามเอาใบสรุปเดือนไปใส่ `importPosByDate` เด็ดขาด** — ตัวนั้นบันทึกเป็น "วันเดียว" = บั๊ก §3.12 เป๊ะ

**ของแถม:** `_docRange_()` แยกออกมาจาก `_rangeWarn_` ใช้ร่วมกัน · `writeMonths_()` **เรียงเดือนหลัง merge**
(เดือนที่เติมย้อนหลังจะได้ไม่ไปต่อท้ายสุดผิดลำดับ · เดือนที่อ่าน period ไม่ออกดันไปท้าย ไม่ทิ้ง)

ตรวจด้วย node: ใบเดือน พ.ค. → `{a:2026-05-01, b:2026-05-31}` ✅ ตรงเป้า ·
ใบรายวัน (12–13 ก.ย.) และใบจ่าย 1 สัปดาห์ (1–7 พ.ค.) → ถูกปฏิเสธถูกต้อง ·
อ่านช่วงไม่ออก → `null` · เรียงเดือนแล้วพฤษภาคมเข้าที่ระหว่างเมษายนกับมิถุนายน ✅

📌 **เมล POS อยู่ที่ `saiyangsoi@gmail.com`** (คนละบัญชีกับ `empro959@gmail.com` ที่ห้อง Code ต่ออยู่)
→ ห้อง Code **เช็คเมล POS แทนไม่ได้** ต้องให้เจ้าของดู/รันเองเสมอ

### ✅ ได้ตัวเลขพฤษภาคมจริงแล้ว — 14 ก.ย. (เจ้าของส่ง PDF เข้าแชทตรงๆ)
เมล POS อยู่ `saiyangsoi@gmail.com` ที่ห้อง Code เข้าไม่ได้ → เจ้าของส่ง **PDF 4 ใบ** มาให้อ่านแทน
ทั้ง 4 ใบเป็น **PDF ที่มี text layer** (ไม่ใช่สแกน) อ่านด้วย `pypdf` ได้ตรงๆ **ไม่ต้อง OCR เลย**
ครอบ `01/05/2569 08:30 → 31/05/2569 02:00` ครบเดือน

⚠️ **กับดักการอ่าน PDF ชุดนี้:** text layer ออกมาแบบ **"ค่ามาก่อนป้าย"** (ค่าตัวเลขอยู่บรรทัดก่อน
ชื่อรายการ) — อ่านแบบ "ป้ายแล้วค่า" ตามสัญชาตญาณจะได้ตัวเลขเลื่อนไปทั้งแถว
พิสูจน์ว่าอ่านถูกด้วยการกระทบยอด **4 ทางที่เลขทุกตัวมาจากในใบเอง**:

| กระทบยอด | ผล |
|---|---|
| รวม 2,406,275.54 − ยอดขายจากการชำระหนี้ 30,027.00 | = **2,376,248.54** = ยอดขายจริง ✔ |
| อาหาร 939,963.61 + เครื่องดื่ม 1,303,751.89 + ภาษี 154,779.48 − ส่วนลด 22,106.25 − ส่วนลดท้ายบิล 140.19 | = **2,376,248.54** ✔ |
| ใบค่าใช้จ่าย: ยอดขายรวม 2,376,243.40 − ยอดรวมค่าใช้จ่าย 2,378,204.16 | = **−1,960.76** = คงเหลือ ✔ |
| วิธีชำระ 5 ช่องรวม | = **2,406,349.00** ตรงยอดในวงเล็บที่ใบพิมพ์กำกับ ✔ |

**ตัวเลขพฤษภาคม 2569:**
- ยอดขาย **2,376,243.40** (เข้าช่วงเดือนอื่น 2.2–2.4 ล้าน พอดี)
- ค่าใช้จ่าย **2,378,204.16** · คงเหลือ **−1,960.76**
- วิธีชำระ: Prompay 2,005,407 · เงินสด 309,065 · บัตรเครดิต 40,845 · **ค้างชำระ 33,919** · **อื่นๆ 17,113**
- ลูกค้า 2,936 คน (809.40 × 2,936 ≈ 2,376,398 ✔ ใกล้ยอดขายจริง)

🎯 **เดือนนี้ยืนยัน §3.20 ด้วยตัวเลขจริง** — รายงานแบบเดิม 3 ช่องจะโชว์แค่ 2,355,317
**ซ่อน 51,032 บาท** (ค้างชำระ + อื่นๆ) ไปเฉยๆ

**`addMonthManual()`** — ใหม่: เติมเดือนด้วยตัวเลขที่กระทบยอดแล้ว **ไม่ใช้ OCR ไม่ค้นเมล** กดครั้งเดียวจบ
⚠️ จงใจไม่ใส่ `sales_categories` — ในใบมีแต่ยอด **ไม่รวมภาษี** รายหมวด ไม่มีภาษีแยกรายหมวด
ถ้าใส่ทั้งที่ไม่ครบ ตัวที่เอาไปรวมจะได้ยอดต่ำกว่าจริง **132,533.04 แบบเงียบๆ**
(อาหาร 939,963.61 + เครื่องดื่ม 1,303,751.89 = 2,243,715.50 = ยอด **ไม่รวมภาษี** ·
ต่างจากยอดขายจริง 2,376,248.54 เท่ากับ ภาษี 154,779.48 − ส่วนลด 22,106.25 − ส่วนลดท้ายบิล 140.19 เป๊ะ)
(เช่นเดียวกับ `daily_totals`/`payment_days`/`expense_records` — ไม่มีข้อมูลพอ ก็ไม่ใส่)

⚠️ **"ค่าใช้จ่าย" ของ POS = เงินจ่ายออกจากลิ้นชัก** (รวมค่าของ · ค่าแรง · เก็บออม) ไม่ใช่ค่าใช้จ่าย
ทางบัญชี — **คงเหลือติดลบเล็กน้อยไม่ได้แปลว่าเดือนนั้นขาดทุนจริง** แต่คิดแบบเดียวกับ ก.ค./ส.ค.
ที่ dashboard ใช้อยู่ จึงเก็บเลขนี้ไว้เพื่อความสอดคล้อง

### ✅ ยืนยันบน production แล้ว — 14 ก.ย. 18:50-18:51
เจ้าของ paste แล้วรัน `addMonthManual()` → `debugDaily()` · log จริง:
```
✅ เติม พฤษภาคม 2569 แล้ว — ยอดขาย 2,376,243  ค่าใช้จ่าย 2,378,204  คงเหลือ -1,961
   saisang_data.json มี 9 เดือนแล้ว · ไม่ได้แตะ pos_daily.json
──────────
🔎 ตรวจวันที่ขาด ทุกเดือนที่มีข้อมูล (3 เดือน):
   • 2026-07: มี 29 วัน · ขาด 0 วัน ✅ ครบ  (ไม่นับวันร้านหยุด 2 วัน: 2026-07-29, 2026-07-30)
   • 2026-08 / 2026-09: ✅ ครบ
   • พฤษภาคม 2569 → ยอดขาย: 2,376,243     ← โผล่ระหว่างเมษายนกับมิถุนายน
✅ เดือนต่อเนื่องกันครบ ไม่มีเดือนไหนหายทั้งเดือน
```
**ผ่านทั้ง 3 อย่างที่ตั้งใจ:** ตัวตรวจดูทุกเดือน (ไม่ใช่เดือนล่าสุด) · ก.ค. รายงานว่าครบ
พร้อมบอกว่าหักวันร้านหยุดวันไหน (ไม่ต้องเดาอีกว่า 29/31 คือครบหรือขาด) ·
`writeMonths_` เรียงเดือนจริง พฤษภาคมเข้าที่ถูกลำดับ ไม่ไปต่อท้ายสุด

**ผลกระทบต่อตัวเลขที่เจ้าของเห็น:**
| | |
|---|---|
| ยอดรวมทั้งปีก่อนเติม | 17,056,925 |
| **หลังเติม** | **19,433,168** (+2,376,243 = **+13.9%**) |
| พ.ค. เทียบค่าเฉลี่ย 8 เดือนเต็ม (2,306,691) | **+3.0%** — เข้าช่วง 2.20–2.38 ล้าน ปกติ |

ทุกกราฟ/สรุปที่ดูข้ามเดือนเคยขาดไป **13.9%** โดยไม่มีสัญญาณอะไรเลย

### 🔴 ค้างไว้ (ไม่บล็อกอะไร)
- ยังไม่ได้แกะ `expense_records` รายการยิบย่อย 29 หน้าของใบค่าใช้จ่าย พ.ค. (ยังไม่จำเป็น —
  ยอดรวมมีแล้ว · กราฟค่าใช้จ่ายรายหมวดของ พ.ค. จะว่าง เหมือน ม.ค.–มิ.ย.)
- **ม.ค.–มิ.ย. ยังไม่มี `daily_totals`/`payment_days` ทั้ง 6 เดือน** — ถ้าวันหนึ่งอยากได้กราฟรายวัน
  ครึ่งปีแรก ต้องกู้ทั้ง 6 เดือน (`importPosByDate` + `monthDates_`) ไม่ใช่เดือนเดียว
- 🪤 **เมล POS อยู่ `saiyangsoi@gmail.com`** ห้อง Code (ต่อ `empro959@gmail.com`) เข้าไม่ได้ —
  ทางที่เวิร์กจริงคือ **เจ้าของส่ง PDF เข้าแชทตรงๆ** (ไฟล์ POS มี text layer อ่านได้ ไม่ต้อง OCR) ใบสรุป 1–31 พ.ค. จาก POS ส่งเข้าเมล
  แล้วรัน `importMonthTotal()` (YM='2026-05') · **ยังไม่เคยรันกับใบจริงสักครั้ง**
- ทางเลือกกู้รายวันยังอยู่ (`importPosByDate` + `monthDates_('2026-05')`) ถ้าวันหนึ่งอยากได้กราฟรายวัน
  ~124 OCR ชนโควตาหลายรอบ — แต่ถ้าจะทำ ต้องกู้ ม.ค.–มิ.ย. ทั้ง 5 เดือน ไม่ใช่เดือนเดียว
- ⚠️ **`backfillPos()` ใช้กู้พฤษภาคมไม่ได้** — มี `allowYM` อนุญาตเฉพาะเดือนที่มีใน `pos_daily`
  (ก.ค./ส.ค./ก.ย.) + เดือนปัจจุบัน · จงใจกันไม่ให้แตะเดือนที่ปลดไปใช้ Excel · **ต้องใช้ `importPosByDate` เท่านั้น**
- ต้อง **paste มือ** เข้า Apps Script เหมือนเดิม

---

---

## 3.22 🚨 เตือนหลอกว่าเงินขาด 75,754 บาท — รูปใบสั่งของถูกตีเป็นสมุดจด — 15 ก.ย. 2026 (PR รอ merge)

### เกิดอะไรขึ้น
15 ก.ย. 22:34 กลุ่ม Management พนักงานส่งรูป **ใบสั่งซื้อของ** (ตารางเส้นบรรทัดเขียนมือ
รายการสินค้าเป็นแถวๆ + จำนวน · ท้ายใบเขียน 1844) → บอทตอบ:
```
⚠️ จดมือ vs POS ไม่ตรง — 2026-08-15
พบ 2 จุดที่ไม่ตรง
1) เงินสด   จด 0 · POS 8,391   → จดน้อยกว่า POS 8,391 บาท
2) เงินโอน  จด 0 · POS 67,363  → จดน้อยกว่า POS 67,363 บาท
รวมรับ 3 ช่อง: จด 0 · POS 75,754
ส่วนต่างสุทธิ (จด−POS): -75,754 บาท
```
**ไม่มีเงินหายเลยสักบาท** — เจ้าของทักว่า *"อันนี้มันก็ไม่ใช่สรุปยอดขายนะ มันเป็นยอดซื้อของ"*

### ต้นเหตุ 3 ชั้น ที่ซ้อนกันจนออกมาเป็นตัวเลขน่ากลัว
**1. `0` ถูกใช้แทน "ไม่รู้" แล้วปลายทางคิดเป็น "จดไว้ 0 บาท"** ← ตัวการหลัก
พรอมป์ต `extract_ledger` สั่งไว้เองว่า *"ถ้าไม่ชัดจริงๆ ใส่ 0 (อย่าเดา)"* แต่ `_recon_emit()`
ข้ามช่องเฉพาะเมื่อ **ทั้งจดมือและ POS เป็น 0** — จด 0 (อ่านไม่ออก) ส่วน POS ไม่ศูนย์
จึงกลายเป็น "จดน้อยกว่า POS" ทุกช่อง
⚖️ **ละเมิดกติกาโปรเจกต์ตรงๆ 2 ข้อ: `0 ≠ ไม่รู้` และ `สัญญาณหลอกแย่กว่าไม่เตือน`**

**2. ด่าน `is_ledger` หลวม** — พรอมป์ตบอกแค่ "ตาราง/สมุดเขียนด้วยลายมือที่สรุปรับ-จ่ายรายวัน"
ใบสั่งของก็เป็นตารางเขียนมือ → ผ่านด่าน · **ตารางเขียนมือ ≠ สมุดยอดรับ**

**3. วันที่เป็น `2026-08-15` ทั้งที่ส่งวันที่ 15 ก.ย.** — วันตรง (15) เดือนเพี้ยน (08 vs 09)
🪤 **กับดักเดียวกับ §3.19 เป๊ะ** (ส.ค. ↔ ก.ย. ต่างกันตัวเดียว) · §3.19 ปิดไป 2 เส้น
(พรอมป์ตสลิปรายรับ · พรอมป์ตเจ้าหนี้) แต่ **พรอมป์ต `extract_ledger` เป็นเส้นที่ 3 ที่ไม่เคยปิด**
→ บทเรียน §3.18 ซ้ำรอบที่ 3: **แก้ทางเดียว บั๊กเดิมไปโผล่ทางที่เหลือ**

### แก้ 3 ชั้น (ชั้นละประตู)
1. **ทุกช่องเป็น 0 = อ่านไม่ออก → ไม่เอาไปเทียบ POS เลย** และ **ตอบในไลน์ว่าอ่านไม่ออก**
   (ห้ามเงียบ — พนักงานต้องรู้ว่ารูปที่ส่งใช้ไม่ได้) พร้อมบอกว่า *"ถ้าเป็นใบสั่งของ ไม่ต้องทำอะไร"*
2. **พรอมป์ต `is_ledger`:** ต้องเห็นช่องฝั่ง 'รับ' ที่มี **'สด' กับ 'โอน' จริงๆ** + เพิ่ม `false`
   สำหรับ *ใบสั่งซื้อของ / ใบจดรายการสินค้า / ยอดซื้อของ / ใบเช็คสต๊อก* พร้อมบอกหน้าตา
3. **ตารางเดือนไทยในพรอมป์ตสมุดจด** (เส้นที่ 3) + คู่ที่สลับบ่อยรวม **ส.ค.(08) ↔ ก.ย.(09)** ·
   *เลขเดือนอ่านไม่ชัดให้ใส่ null **ห้ามเดา*** — เดาเดือนผิด = เทียบกับ POS ของผิดวันทั้งใบ
4. **ธงในข้อความเตือน:** วันที่ในสมุดย้อนหลังเกิน `RECON_OLD_WARN_DAYS` (7) วัน → ขึ้นบรรทัด
   *"ย้อนหลัง N วัน — ถ้าไม่ใช่วันที่ในสมุดจริง บอทอาจอ่านเลขเดือนผิด"* (คนอ่านจะจับได้เอง
   ไม่ต้องรอให้สังเกตวันที่ในหัวข้อ)

### ระดับการตรวจ
- เทสต์ใหม่ **9 เคส** (297/297 เขียว) รันผ่าน `_process_recon_image()` ตัวจริง — คุมสองทิศ:
  ทุกช่อง 0 ต้องไม่เข้าคิว **และต้องตอบในไลน์** · **อ่านออกบางช่องยังต้องเทียบตามปกติ**
  (จดสดแต่ไม่มีโอนเลยเป็นไปได้จริง ห้ามทิ้ง) · `is_ledger=false` ยังเงียบเหมือนเดิม
  (ไม่งั้นบอทจะตอบทุกรูปในกลุ่ม = สแปม) · ค่า `None`/ติดลบต้องไม่พัง
- ⚠️ **ยังไม่ยืนยันบน production** — ต้องลองส่งรูปใบสั่งของซ้ำหลัง deploy

---

---

## 3.28 🫀 สัญญาณชีพคอนเทนต์เงียบ 10 วัน — ไม่ใช่ env ตั้งไม่ครบ (ผมเดาผิด) — 17 ก.ย. 2026 (PR รอเจ้าของ)

### ผมเดาผิดเรื่องนี้ ต้องบันทึกไว้
ตั้งแต่ทำฟีเจอร์นี้ (7 ก.ย.) **บรรทัด "🫀 สัญญาณชีพคอนเทนต์" ไม่เคยโผล่ในรายงาน 00:30 เลย**
ผมสรุปไว้ว่า **"เป็น env ตั้งไม่ครบ"** โดยให้เหตุผลว่า *ถ้าดึงตัวเลขไม่ได้มันจะขึ้น ⚠️ ให้เห็น*
→ **เหตุผลนั้นถูก แต่ข้อสรุปผิด** เพราะมันตัดความเป็นไปได้ที่ว่า "โค้ดไม่เคยเดินมาถึงบรรทัดนั้นเลย"

17 ก.ย. เจ้าของเปิดหน้า Render Environment ให้ดู → **ตั้งครบทั้ง 2 ตัว**
(`CONTENT_HEARTBEAT_GROUP` = `C81b41c6…` · `CONTENT_HEARTBEAT_URL` = Apps Script `/exec`)

### ต้นเหตุจริง — ฟีเจอร์เกาะพาหนะที่ไม่มีอยู่จริง
สัญญาณชีพไม่ได้ส่งเป็นข้อความของตัวเอง มันไป **ต่อท้ายรายงาน 00:30 ของกลุ่มนั้น**:
```python
hb_lines = _content_heartbeat_lines(report_date) if group_id == CONTENT_HEARTBEAT_GROUP else []
```
แต่เป้ารายงาน 00:30 มาจาก `SELECT group_id FROM groups` — ตารางที่เขียนตอน **บันทึกสลิป/บิล**
**ห้องคอนเทนต์ไม่เคยส่งสลิป → ไม่มีแถวในตาราง `groups` → ไม่เคยเป็นเป้ารายงาน**
→ `build_daily_report()` **ไม่เคยถูกเรียกด้วย group นั้นเลย** → ไม่มีอะไรให้ต่อท้าย
และไม่มี log สักบรรทัดที่บอกเรื่องนี้ (ตระกูลเดียวกับ §3.20 / §3.26)

📌 **บทเรียน: ฟีเจอร์ที่เกาะพาหนะของคนอื่น ต้องเช็คว่า "พาหนะวิ่งจริงไหม" ไม่ใช่แค่ตั้งค่าครบ**
📌 **บทเรียนของผมเอง: "ถ้าพังต้องมีเสียง" ใช้ได้เฉพาะกับโค้ดที่ถูกเรียก — ความเงียบแบบที่**
   **"ไม่เคยถูกเรียก" ดังไม่ได้เลย จึงห้ามใช้ความเงียบมาตัดสมมุติฐาน**

### แก้
- แยกตัวเลือกเป้ารายงานออกมาเป็น `_report_targets()` (เทสต์ได้ตรงๆ ไม่ต้องรัน job ทั้งก้อน)
- ถ้าตั้ง `CONTENT_HEARTBEAT_GROUP` + `URL` ครบ แต่กลุ่มนั้นไม่อยู่ในเป้า → **เพิ่มเข้าเป้าเอง**
  (`build_daily_report` มีขา "ไม่มีสลิป" ที่ยังต่อสัญญาณชีพให้อยู่แล้ว — ขานี้เคยเขียนไว้ถูกแล้ว
  แค่ไม่มีใครเรียก)
- ถ้ากลุ่มนั้นถูกกันด้วยเหตุอื่น (เตะออก/IGNORE/ปิดรายงาน/ปิดสลิป) → **เคารพคำสั่ง แต่ log บอกเหตุ**
  (`[heartbeat] ⚠️ กลุ่ม … ไม่ได้รับรายงาน (สั่งปิดรายงานไว้) → สัญญาณชีพไม่ออก`)

### ระดับการตรวจ
เทสต์ใหม่ **5 เคส (335/335 เขียว)**: กลุ่มไม่เคยมีสลิปต้องเข้าเป้า + ต้องมี log ·
ไม่ตั้ง env ต้องไม่ยัดกลุ่มแปลกเข้ามา (พฤติกรรมเดิมเป๊ะ) · สั่งปิดรายงานต้องไม่ฝืนส่งแต่ต้องบอกเหตุ ·
กลุ่มที่มีสลิปอยู่แล้วต้องไม่ถูกเพิ่มซ้ำ · รายงาน "ไม่มีสลิป" ต้องยังมีบรรทัดสัญญาณชีพ

### ⚠️ ผลข้างเคียงที่เจ้าของต้องรู้ก่อน merge
หลัง merge ห้องคอนเทนต์จะ **เริ่มได้ข้อความ 00:30 ทุกคืน** (หน้าตา `📊 ไม่มีสลิปวันที่ …`
\+ บล็อกสัญญาณชีพ) ซึ่งเป็นสิ่งที่ตกลงกันไว้ตั้งแต่ 19 ส.ค. — ถ้าไม่ต้องการให้เห็นบรรทัด
"ไม่มีสลิป" ต้องแก้เพิ่ม (ให้ขานั้นส่งเฉพาะบล็อกสัญญาณชีพ)

---

## 3.27 📅 ไล่ "ตาข่ายวันที่" ให้ครบทุกเส้น — เจอเส้นจองไม่มีตาข่ายเลย — 17 ก.ย. 2026 (PR รอ merge)

### ทำไมถึงต้องทำ
§3.23 (เดือนเพี้ยน ก.ย.→ก.ค.) และ §3.25 (ปีเพี้ยนเป็น 2024) เป็น**เรื่องเดียวกันคนละเส้น**:
วันที่ที่ AI อ่านมาลง DB โดยไม่มีใครตรวจว่า "เป็นไปได้ไหม" → เลยไล่**ทุกเส้นที่เก็บวันจาก AI** ทีเดียว

| ตารางที่เก็บ | ช่องวันที่ | ที่มา | ตาข่ายเดิม |
|---|---|---|---|
| `payable_bills` | `doc_date` | AI | ✅ `_sane_doc_date` (§3.12) |
| `payable_payments` | `doc_date` | AI | ✅ `_sane_doc_date` + `_payment_date_contradicts` (§3.23) |
| `payable_payments` | `slip_datetime` | AI | ❌ **ไม่มีเลย — ส่ง `info['datetime']` ดิบๆ ลง DB** |
| `slips` | `slip_datetime` | AI | ✅ `_sane_slip_dt` (§3.25) |
| `slips` | `slip_date` | นาฬิกา/คนพิมพ์ | ✅ ไม่ใช่ AI |
| `recon_pending` | `date_iso` | AI | ✅ `_sane_doc_date` |
| `reservations` | **`resv_date`** | AI | ❌ **มีแค่ `_valid_ymd` = เช็ค "รูปแบบ" ไม่ได้เช็ค "เป็นไปได้"** |
| `image_misses` · `dining_bills` · `dining_pairs` · `meta` | วันที่ | นาฬิกาเครื่อง | ✅ ไม่ใช่ AI |

### 🔴 เส้นจองคือเส้นที่อันตรายที่สุดในตาราง — เพราะมันเงียบ
ถ้า AI คิดปี/เดือนเพี้ยน (พ.ศ.→ค.ศ. พลาด · `'16/7'` ของปีที่แล้ว) วันนั้น**ลง DB ได้สบาย** แล้ว:
1. **วันอยู่ในอดีต** → ไม่โผล่ในสรุป "จองวันนี้" (query `resv_date = วันนี้`) และ `_cleanup_old_data`
   **ลบทิ้งทันที**ที่เก่าเกิน `RESV_KEEP_DAYS` (ปีเพี้ยน = โดนลบรอบถัดไปเลย)
2. **วันอยู่อนาคตไกล** → ไม่โผล่ทั้งสรุปวันนี้และวันงานจริง

ทั้งสองทาง **การ์ดเด้งปกติ พนักงานเห็นว่า "จองเข้าแล้ว" — แล้วจองหายโดยไม่มีเสียงอะไรเลย**
(เทียบกับเส้นเจ้าหนี้ที่ถ้าวันเพี้ยนยังเห็นยอดในสรุปหนี้ผิดที่ผิดทาง = ยังมีร่องรอย)

### แก้
- **`_sane_resv_date(rd)`** — จองคือเรื่อง**อนาคต**: รับย้อนหลังได้แค่ `RESV_PAST_GRACE_DAYS` (1 วัน
  — พิมพ์ `'29/8'` ตอนดึกของ 30/8 ยังปกติ) และล่วงหน้าไม่เกิน `RESV_AHEAD_MAX_DAYS` (365 วัน)
  · ไม่ผ่าน → คืน `None` + log บอกเหตุ (`ห่างจากวันนี้ N วัน`)
- **ปลายทางต้อง "ถาม" ไม่ใช่ "เดาเป็นวันนี้"** — ต่างจากเส้นเจ้าหนี้ที่ทิ้งวันแล้วใช้วันนี้แทนได้
  (ยอดไม่พึ่งวันที่) แต่**จองผิดวัน = ลูกค้ามาผิดวัน/โต๊ะไม่ว่าง** → ทิ้งวันนั้นแล้วตกไปที่
  `missing = ['date']` ซึ่งเป็นทางถามกลับที่มีอยู่แล้ว ("วันไหนครับ?")
- เสียบด่านไว้ **4 จุด**: `handle_reservation_text` (หลักการ — ถามกลับ) · `save_reservation`
  (เส้นสุดท้ายก่อน INSERT → เก็บ `NULL` ดีกว่าเก็บวันผิด) · `_find_dup_reservation` (เทียบจองซ้ำ)
  · ขา "เดี๋ยวมา/ซักครู่" (ข้อความบอกชัดว่ามาวันนี้ → ทับด้วยวันนี้ได้ ไม่ต้องกวนถาม)
- `payable_payments.slip_datetime` → กรองด้วย `_sane_slip_dt` เหมือนเส้นสลิปรายรับ
  (ช่องนี้ยังไม่มีใครอ่านไปคำนวณ แต่เป็นหลักฐานในบัญชีเงิน — วันผิด = หลักฐานผิด)

### ระดับการตรวจ
เทสต์ใหม่ **16 เคส (330/330 เขียว)** — ครบทั้งสองทิศตามบทเรียน §3.24:
- ทิศ "ต้องกัน": ปีเพี้ยน 2 ปี · ย้อนหลัง 400/730 วัน · ล่วงหน้า 800/900 วัน · รูปแบบพัง → ต้องไม่ลง DB
- ทิศ "ห้ามกินของจริง": วันนี้ · พรุ่งนี้ · +30 · +180 · ล่วงหน้าไกลสุดที่อนุญาต · เมื่อวาน → ต้องผ่าน
- end-to-end ผ่าน `handle_reservation_text`: วันเพี้ยน → **ถามวันที่ + ไม่บันทึก** · จองพรุ่งนี้ปกติ →
  **บันทึก + การ์ดออก** · "เดี๋ยวมา" + วันเพี้ยน → **ลงเป็นวันนี้ ไม่ต้องถาม**
- เอกสารความเสียหาย: เขียนวันเพี้ยนลง DB ตรงๆ แล้วรัน `_cleanup_old_data()` → **หายทั้งใบ**
- ด่านต้องมี log บอกเหตุ (กติกา: ด่านที่คัดของออกแบบเงียบ = ไล่เหตุไม่ได้)

📌 **บทเรียนรวม 3 ตอน (§3.23 → §3.25 → §3.27): พรอมป์ตคือการขอร้อง ไม่ใช่การรับประกัน**
ของที่กระทบเงิน/จองต้องมีตาข่ายในโค้ดเสมอ — และ**ตาข่ายต้องไล่เป็น "ตาราง" ไม่ใช่ทีละเส้นตอนพัง**
(3 ตอนนี้คือเส้นเดียวกันหมด ไล่ทีเดียวจบตั้งแต่ตอนแรกก็ได้)

---

## 3.26 🔎 log ของ recon ไม่บอกว่า "เห็นรูปเป็นอะไร" — เลยต้องเดา 2 วัน — 17 ก.ย. 2026 (PR รอ merge)

### ทำไมถึงต้องทำ
17 ก.ย. 12:43 (05:43 UTC) มีรูป 3 ใบเข้ากลุ่มเทียบยอด → `[recon] ไม่ใช่สมุดจดมือ (ข้าม)` ทั้ง 3 ใบ
**deploy ที่คลายด่านแล้ว (§3.24 · merge 16 ก.ย. 10:56 UTC) ขึ้นไปก่อนหน้านั้นแล้ว** — แต่**ยังบอกไม่ได้**
ว่าเป็น "คัดถูก (ใบสั่งของ/ใบเสร็จ)" หรือ "คัดผิด (สมุดจดแท้)"

🪤 **ต้นเหตุคือตัว log เอง** — เขียนแค่ `ไม่ใช่สมุดจดมือ (ข้าม)` ไม่บอกว่ารูปนั้นเป็นอะไร
→ **2 วันที่ไล่เรื่องนี้ ต้องเดาทุกครั้ง** และแก้พรอมป์ตไป-กลับ 2 รอบ
(§3.22 รัดแน่นเกิน → §3.24 คลายกลับ) ทั้งที่ข้อมูลที่ต้องใช้ตัดสินใจ AI มีอยู่ในมือแล้ว

📌 **บทเรียน: เครื่องมือวินิจฉัยที่ไม่บอกว่า "เห็นอะไร" = ต้องเดาต่อไปเรื่อยๆ**
คู่กับ §3.20 (`[payable]` เงียบทั้งเส้น) — ตระกูลเดียวกัน: **ขาที่ตัดสินใจต้องบอกเหตุผลเสมอ**

### แก้
- พรอมป์ต `extract_ledger` เพิ่มฟิลด์ **`saw`** = "รูปนี้คืออะไร" สั้นๆ ไม่เกิน 12 คำ
  **ตอบทุกครั้งแม้ `is_ledger=false`** (เช่น *'ใบสั่งซื้อของเขียนมือ'*, *'ใบเสร็จ POS พิมพ์'*, *'รูปอาหาร'*)
- log ขาคัดออก: `ไม่ใช่สมุดจดมือ (ข้าม) group=... เห็นเป็น='ใบสั่งซื้อของเขียนมือ'`
  · AI ไม่ตอบ → เขียน `'(AI ไม่บอก)'` ไม่เว้นว่างให้เดาว่าลืม log
- **log ขาที่รับเป็นสมุดจด** ก็เพิ่มด้วย: `รับเป็นสมุดจด วันที่=... สด=8,391 โอน=67,363 บัตร=0`
  → **จับ "อ่านเลขผิด" ได้ทันทีจาก log ไม่ต้องรอผลเทียบตอนเช้า**

### ระดับการตรวจ
เทสต์ใหม่ **4 เคส** (314/314 เขียว): คัดออกต้องบอกว่าเห็นเป็นอะไร · AI ไม่บอกต้องรู้ว่าไม่บอก ·
รับเป็นสมุดจดต้องโชว์เลขที่อ่านได้ · พรอมป์ตต้องบังคับ `saw` แม้ตอบว่าไม่ใช่สมุด

### ✅ ปิดแล้ว — เจ้าของยืนยัน 17 ก.ย. 2026
**รูป 3 ใบเมื่อ 17 ก.ย. 12:43 "เป็นรูปสลิปอื่น"** (ไม่ใช่สมุดจด) → **ด่าน `is_ledger` คัดถูกแล้ว**
ไม่มีจดมือหลุด · ไม่มีอะไรต้องแก้เพิ่มในด่านนี้

📌 แต่บทเรียนยังอยู่: **ตอนนั้นตอบคำถามนี้เองไม่ได้** ต้องรอเจ้าของยืนยัน — และเพราะเดาไม่ออก
จึงไปแก้พรอมป์ตไป-กลับ 2 รอบ (§3.22 รัด → §3.24 คลาย) โดยที่ **ของจริงไม่ได้ผิดตั้งแต่ต้น**
ค่า `saw` ที่ใส่ไปรอบนี้คือสิ่งที่ทำให้รอบหน้าไม่ต้องถามใคร
**ห้ามแก้พรอมป์ตด่านนี้อีกจนกว่าจะเห็นค่า `saw` จริงจาก log**
(ยังน่าคิดต่อ: §3.24 คลายด่านไปไกลกว่าที่จำเป็นหรือเปล่า — แต่ห้ามแก้จากการเดา รอ `saw` ก่อน)

---

## 3.25 🚨 เตือน "สลิปปลอม" ทั้งที่เป็นเงินจริง — วันเวลาบนสลิปลงปี 2024 — 17 ก.ย. 2026 (PR รอ merge)

### เกิดอะไรขึ้น
17 ก.ย. 12:59 ลูกค้าโอน **350 บาท** เข้า บจก.ไส้ย่างซอย4 (สลิป K+ · `17 ก.ย. 69 12:59 น.` ·
ref `016260125933AQR09626`) → บอทเตือน:
```
🚨 [ALERT] สลิปปลอมจาก ?!
🔴 เลขอ้างอิง 016260125933AQR09626 เคยถูกส่งมาแล้ว! (สลิปซ้ำ)
   ↳ ซ้ำกับใบก่อน: นาย บัณฑิต ต ศ · เวลา 2024-09-17T12:59:00 · อ้างอิง 016260125933AQR09626
```

**เงินจริง ไม่ใช่ปลอม** — "ใบก่อน" ที่มันอ้างคือ **ใบเดียวกันนี้เอง** (ref เดียวกัน · ยอด 350 เท่ากัน ·
เวลา 12:59:00 เหมือนกัน · ชื่อผู้โอนเกือบเหมือน ต่างแค่ OCR) แต่**ปีในฐานข้อมูลเป็น 2024**
ทั้งที่สลิปเขียน `69` = พ.ศ. 2569 = **ค.ศ. 2026** (น่าจะอ่าน 69 เป็น 67 → 2567−543 = 2024)

### ต้นเหตุ 2 จุด
**1. `slip_datetime` ไม่มีด่านตรวจเลย** — `save_slip()` เก็บ `info['datetime']` จาก AI **ตรงๆ**
ต่างจากเส้นเจ้าหนี้ที่มี `_sane_doc_date` มาตั้งแต่ §3.12 → ปีผิดไป 2 ปีลง DB ได้สบาย
แล้วพังต่อ **2 ทาง**: ตัวจับสลิปซ้ำ (`amount_time`) เทียบกับวันที่ผิด · ข้อความเตือนอ้างวันที่ที่ไม่มีจริง
🪤 **เส้นสลิปรายรับเป็นเส้นที่ 4 ที่ยังไม่มีตาข่ายวันที่** (§3.19 ปิดเจ้าหนี้+รายรับระดับพรอมป์ต ·
§3.22 ปิดสมุดจด · แต่ **ตาข่ายในโค้ด** มีแต่เส้นเจ้าหนี้เท่านั้น)

**2. พาดหัว "สลิปปลอม" ใช้กับทุกเคสที่ไม่ผ่าน** — รวมเคส "ส่งใบเดิมซ้ำ" ซึ่งเกิดบ่อยและไม่มีพิษภัย
⚖️ **เตือนเกินจริงบ่อยๆ = พนักงานเลิกอ่าน แล้ววันที่ปลอมจริงจะไม่มีใครสนใจ**

### แก้ 2 จุด
1. **`_sane_slip_dt()`** — วันเวลาบนสลิปต้องไม่เป็นอนาคต (เผื่อ 1 วัน สำหรับโอนข้ามเที่ยงคืน)
   และไม่เก่าเกิน `SLIP_DT_MAX_DAYS` (120) · ไม่ผ่าน = **เก็บเป็น `None` = "ไม่รู้"** ดีกว่าเก็บค่าที่ผิด
   (ยอมให้ตัวจับซ้ำแบบ `amount_time` อ่อนลงสำหรับใบนั้น — แลกกับไม่สร้างวันที่ปลอมในบัญชี)
2. **พาดหัวตามสิ่งที่พิสูจน์ได้:**
   | เคส | พาดหัวใหม่ |
   |---|---|
   | ref เดียวกัน **ยอดไม่ตรง** (พิสูจน์ได้ว่าถูกแก้) | `🚨 สลิปถูกตัดต่อ` |
   | ref/ยอด **ตรงกันเป๊ะ** (ส่งใบเดิมซ้ำ) | `⚠️ สลิปส่งซ้ำ` + *"ถ้าเป็นใบเดิมที่ส่งซ้ำ ไม่ต้องทำอะไร — บอทไม่นับซ้ำให้แล้ว"* |
   | อื่นๆ | `🚨 สลิปต้องสงสัย` (ไม่เหมาว่า "ปลอม" ซึ่งบอทพิสูจน์ไม่ได้) |

### ระดับการตรวจ
- เทสต์ใหม่ **9 เคส** (310/310 เขียว) คุมสองทิศ: ปีเพี้ยน 2 ปี/อนาคตไกล → ไม่เก็บ ·
  **ย้อนหลัง 10 วัน และอนาคต 6 ชม. ยังเก็บตามเดิม** (ส่งสลิปย้อนหลัง/โอนข้ามเที่ยงคืน = ปกติ ห้ามตัด) ·
  **เคสตัดต่อยังต้องดังเหมือนเดิม ห้ามลดเสียง**
- ⚠️ ยังไม่ยืนยันบน production

### 🔴 ค้างไว้
- **ยังไม่รู้ว่าทำไมใบเดียวกันถูกประมวลผล 2 รอบ** (พนักงานส่ง 2 ครั้ง? / ตัวกู้สลิปอ่านซ้ำ?)
  — ตาข่ายกันนับซ้ำทำงานถูกอยู่แล้ว (ไม่นับเงินซ้ำ) แต่สาเหตุต้นทางยังไม่ได้ไล่
- **แถวปี 2024 ที่ค้างใน DB** ยังอยู่ (ใบใหม่จะไม่เกิดอีก แต่ใบเก่าไม่ได้แก้ย้อนหลัง)

---

## 3.24 🔙 รัดพรอมป์ตแน่นเกินไป → สมุดจดของจริงถูกคัดออก — 15-16 ก.ย. 2026 (PR รอ merge)

**ผมทำพังเอง** — §3.22 (deploy 15 ก.ย. 23:12) เพิ่มเงื่อนไขว่า `is_ledger=true` ต้อง
*"เห็นคอลัมน์/ช่องฝั่ง 'รับ' ที่มี 'สด' กับ 'โอน' จริงๆ"* → **สมุดจดแท้ๆ ถูกคัดออก 3 ใบติด**

log ยืนยันชัด (หลัง deploy 16:13 UTC):
```
16:17:39  [recon] ไม่ใช่สมุดจดมือ (ข้าม) group=C91b4fc6...
16:53:5x  [recon] ไม่ใช่สมุดจดมือ (ข้าม) group=C91b4fc6...  (อีก 2 ใบ)
```
ไม่มี `เข้าคิวเทียบยอด` / `อ่านยอดรับไม่ได้เลย` เลยสักบรรทัด = **ตกด่านแรกทั้งหมด**
เจ้าของว่า *"ส่งจดมือเมื่อกี้เข้าไปแล้วเงียบ"* — ตรงกับ log เป๊ะ

**สาเหตุ:** ถ่ายเอียง / ครอปหัวตาราง / หัวคอลัมน์จาง → เงื่อนไขนี้ไม่ผ่าน แม้เป็นสมุดจดแท้

### แก้ — คลายด่านแรก ให้ตาข่ายในโค้ดทำงานแทน
คืนคำอธิบายเป็นแบบกว้าง + บอกชัดว่า *"หัวคอลัมน์อาจจาง/ถูกครอป/ถ่ายเอียงจนไม่เห็น —
ถ้าโดยรวมเป็นสมุดสรุปเงินรายวันก็ตอบ true ได้"* · **คงข้อที่ตัดใบสั่งซื้อของไว้** (ข้อนั้นเจาะจงและถูก)

📌 **ยึดบทเรียน §3.23 ตรงๆ: พรอมป์ตคือการขอร้อง ไม่ใช่การรับประกัน**
→ จึง **ไม่ควรรัดพรอมป์ตจนตัดของจริงทิ้ง** เพราะมีตาข่ายในโค้ดอยู่แล้ว
(ทุกช่องเป็น 0 → ไม่เทียบ + ตอบว่าอ่านไม่ออก) · **ปล่อยด่านแรกกว้าง ให้โค้ดคัดปลายทาง
ปลอดภัยกว่าคัดทิ้งตั้งแต่ต้นทางแบบเงียบๆ** — คัดผิดที่ต้นทาง = เงียบ ไม่มีใครรู้ว่าของหาย

⚠️ **บทเรียนซ้อน: การรัดด่านให้แน่นเพื่อกัน false positive สร้าง false negative ที่เงียบกว่าเดิม**
§3.22 แก้ "เตือนหลอก" แต่เผลอสร้าง "ของจริงหายเงียบ" ซึ่งอันตรายกว่าตามกติกาโปรเจกต์เอง
ต่อไปเวลารัดด่าน ต้องถามก่อนว่า *"ถ้ารัดพลาด ของจริงที่หลุดไปจะส่งเสียงไหม"*

### ระดับการตรวจ
- เทสต์เดิมที่ล็อกถ้อยคำเข้มถูกเปลี่ยนเป็น **assertNotIn** พร้อม docstring บอกว่าทำไม
  (กันคนมาใส่เงื่อนไขแน่นแบบเดิมอีก) — 301/301 เขียว
- ⚠️ **ยังไม่ยืนยันบน production** — ต้องส่งสมุดจดใหม่แล้วดูว่าได้ `เข้าคิวเทียบยอด` ไหม

---

## 3.23 📅 ข้อความ "วันที่เพี้ยน" ทำให้เข้าใจผิดว่าตัดยอดผิด — 16 ก.ย. 2026 (PR รอ merge)

### เกิดอะไรขึ้น (ระบบทำถูกหมด แต่ข้อความสื่อสารพลาด)
16 ก.ย. 00:16-00:17 สลิป 2 ใบจ่ายดวงใจ (3,615 · 8,231) — วันบนสลิปเขียน `16 ก.ย. 69`
บอทตอบ:
```
📅 วันบนสลิปอ่านได้เป็น 16/07/26 ซึ่งย้อนไปก่อนบิลที่จ่าย (14/09/26)
   — บอทอ่านเดือนเพี้ยน จึงลงวันที่เป็น 16/09/26 (วันที่ส่ง) แทน
```
แล้วสรุปหนี้ตัดยอดเข้าบิล **14/09 และ 15/09 ถูกทั้งคู่**

เจ้าของอ่านแล้วงง: *"ไม่เข้าใจ บอกว่าอ่านไม่ได้แต่ตัดยอดถูก"*

### ทำไมทั้งสองอย่างจริงพร้อมกัน — 3 ช่องนี้เป็นอิสระต่อกัน
| ช่อง | อ่านจากไหน | รอบนี้ |
|---|---|---|
| **วันโอน** (`doc_date`) | ตัวเลขวันที่บนหน้าสลิป | ❌ อ่าน ก.ย. เป็น ก.ค. |
| **บิลที่ตัด** (`pay_for`) | **'บันทึกช่วยจำ' บนสลิป — คนละช่องกันเลย** | ✅ ถูก |
| ยอดเงิน | ช่องจำนวนเงิน | ✅ ถูก |

การตัดยอด **ไม่ได้พึ่งวันโอนเลย** → วันที่เพี้ยนจึงไม่ทำให้ตัดผิดใบ
ข้อความเดิมพูดแค่ "อ่านเดือนเพี้ยน" ไม่ได้บอกว่าเพี้ยนแค่ช่องไหน → อ่านแล้วคิดว่าผิดทั้งใบ

⚖️ **เครื่องมือที่ทำไว้กันพลาด ห้ามทำให้คนอ่านกังวลเกินเหตุเสียเอง** (คู่กับกติกา
"สัญญาณหลอกแย่กว่าไม่เตือน" — เตือนถูกแต่สื่อสารพลาด ก็กินเวลาคนเหมือนกัน)

### 🔴 ข้อเท็จจริงสำคัญ: ตารางเดือนไทยในพรอมป์ต **ไม่ได้กันได้**
§3.19 (deploy 13 ก.ย.) ใส่ตารางเดือนไทยลงพรอมป์ตเจ้าหนี้แล้ว — **แต่ 16 ก.ย. ยังอ่าน
ก.ย. เป็น ก.ค. อีก** → **ชั้นที่ 1 (พรอมป์ต) เชื่อไม่ได้**
ตัวที่ช่วยไว้จริงคือ **ชั้นที่ 2 `_payment_date_contradicts()`** (วันโอนย้อนก่อนบิลที่จ่าย
= เป็นไปไม่ได้ → ทิ้งวันที่ ใช้วันที่ส่ง) · ถ้ามีแต่ชั้นแรก เงินก้อนนี้จะไปโผล่วันที่ 16/07/26
📌 **บทเรียน: พรอมป์ตคือการขอร้อง ไม่ใช่การรับประกัน — ของที่กระทบเงินต้องมีตาข่ายในโค้ดเสมอ**

### แก้
เขียนข้อความใหม่ให้บอกครบ 3 อย่าง: **อะไรเพี้ยน · อะไรไม่กระทบ · แก้ยังไงถ้าวันจริงไม่ใช่วันนี้**
```
📅 บอทอ่านวันบนสลิปเป็น 16/07/26 ซึ่งย้อนก่อนบิลที่จ่าย (14/09/26) = เป็นไปไม่ได้
   → ทิ้งวันนั้น ลงเป็น 16/09/26 (วันที่ส่ง) แทน
✅ ยอด 3,615 และบิลที่ตัด **ไม่ได้ใช้วันที่นี้** — เรื่องวันที่ไม่ทำให้ตัดผิดใบ
   (บิลที่ตัดอ่านจาก 'บันทึกช่วยจำ' บนสลิป คนละช่องกับวันโอน)
ถ้าวันโอนจริงไม่ใช่ 16/09/26 → พิมพ์ 'ลบจ่าย 16/09/26 3,615' แล้วส่งสลิปใหม่
```

### ระดับการตรวจ
- ✅ **ยืนยันแล้วว่าคำสั่งที่บอทบอก ใช้ได้จริง** — `_parse_thai_date('16/09/26')` → `2026-09-16`
  (รับทั้ง `16/9` · `16/09/26` · `16/09/2569`) และตัวแกะยอดตัดคอมมาออกให้ (`'3,615'` → 3615.0)
- เทสต์ใหม่ **4 เคส** (301/301 เขียว) รวม **เทสต์ที่ดึงคำสั่งออกจากข้อความจริงด้วย regex
  แล้วยิงเข้าตัวแกะวันที่** — ล็อกไว้ว่าคำสั่งที่บอกผู้ใช้ต้องแกะได้และตรงกับวันที่ที่ลงจริง
- แก้เทสต์เก่า §3.19 ที่ยึด **ถ้อยคำเป๊ะ** (`"อ่านเดือนเพี้ยน"`) → เช็ก **ใจความ** แทน
  (ทิ้งวันไหน · ลงวันไหน · แก้ยังไง) เทสต์ที่ผูกกับถ้อยคำทำให้แก้ข้อความให้ดีขึ้นไม่ได้
- ⚠️ ยังไม่ยืนยันบน production

---

---

## 4. แก้อะไรไปแล้ว 17 ส.ค. 2026

### 4.1 บอท `app.py` — 6 commit บน `main` (deploy แล้วทั้งหมด)

| commit | เรื่อง |
|---|---|
| `5c19454` | ส่งรูป **บิลซื้อ** → เด้งสรุปหนี้อัตโนมัติ (เดิมบันทึกเงียบ) + reply พลาดแล้ว fallback push |
| `173b786` | **แก้ตัดบิลผิดใบ**: จ่ายมาตรงยอดบิลใบไหนตัดใบนั้น (ยอดตรงเป๊ะทั้งบัญชี ชนะวันที่บนสลิป) |
| `ee0fd91` | ฟอร์มบล็อก "ค้าง": ปี 2 หลัก `/26`→2026 (เดิมเป็น พ.ศ.→1983) · วงเล็บหลายเลขใช้เลขหลังคำ "ค้าง" · รายงานบรรทัดที่อ่านไม่ได้ (เดิมข้ามเงียบ) |
| `ee2ef5f` | วางบล็อกยอดค้างได้ทุกแบบ — ก๊อปสรุปของบอทมาทั้งดุ้นก็ได้ (เดิมบรรทัดแรกต้องเป็นคำว่า `ค้าง` เป๊ะ ไม่งั้นเงียบสนิท) |
| `462c892` | **ลบบิล/จ่าย ต้องคืนยอดที่จับคู่ไว้** (เดิมยอดค้างเพี้ยน) · `จัดยอดใหม่` ใช้กติกาเดียวกับ settle · แก้ reconcile พังตอนสั่ง 2 ครั้งซ้อน |
| `33ac046` | แก้ `[cleanup] error: tuple index out of range` — psycopg2 ตีความ `%` ใน `LIKE 'sent:%'` เป็น format spec → **การลบข้อมูลเก่าไม่เคยทำงานเลยตั้งแต่ย้ายมา Postgres** |

### 4.1.1 รวม repo กลับมา `empro959` + เพิ่มชุดเทสต์ (ท้ายวัน)

| commit | เรื่อง |
|---|---|
| `bcb7020` | **merge รวม 2 สายที่แยกกันตั้งแต่ `cf1aeb2`** — ตรวจแล้วไม่มีงานฝั่งไหนหาย: `app.py` เอาฝั่ง saiyangsoi ทั้งไฟล์ (อีก 4 commit ของ empro959 คือฟีเจอร์ 'บิลซื้อเด้งสรุป' เรื่องเดียวกัน ทำซ้ำกัน) · dashboard/doc ของ empro959 merge เข้าเองไม่ชน · พารามิเตอร์ `source_only` เป็น dead code จึงไม่รับเข้ามา |
| `51da09e` | **เพิ่ม `tests/` — 18 เคส** ครอบ settle/reconcile/unlink/`_pg_sql`/fallback push (ดูหัวข้อ "เทสต์" ใน `ARCHITECTURE.md`) · ไม่แตะ `app.py` |

**⚠️ สลับต้นทาง deploy: ยังไม่ได้ทำ** — 17 ส.ค. เจ้าของเข้าใจว่าสลับแล้ว ผมเลยจดลงเอกสารตามนั้น
แต่ 18 ส.ค. 23:10 เปิดหน้า Render ดูจริง พบว่ายังต่อกับ `saiyangsoi-sketch/Line-Slip-Bot` อยู่
live ที่ `33ac046` ตั้งแต่ 17 ส.ค. 13:22 → **ทุก commit ที่ push เข้า empro959 หลังจากนั้นไม่เคยขึ้นระบบ**
เสียเวลาไล่บั๊กทั้งคืนเพราะเข้าใจว่าโค้ดใหม่รันอยู่แล้ว

**บทเรียน: อย่าเชื่อคำบอกเล่าเรื่องสถานะระบบ ให้เปิดหน้าคอนโซลดูของจริงเสมอ**
วิธีเช็คใน 5 วินาที: Render → service → ดูบรรทัด GitHub + commit hash ใต้ชื่อ service

### 4.1.2 ปิดงานค้าง 18 ส.ค.

| commit | เรื่อง |
|---|---|
| `893d0d6` | `importPosByDate()` รับหลายวันในรอบเดียว (เดิมฮาร์ดโค้ดวันเดียว ต้องแก้โค้ดแล้วรันซ้ำ) |
| `9058af8` | เพิ่ม `CLOSED_DAYS` — แยก "วันร้านหยุด" ออกจาก "วันข้อมูลหาย" ใน `debugDaily()` |
| `f453191` | เอกสาร: ปิดข้อ UptimeRobot |

⚠️ 2 commit แรกเป็นไฟล์ **Apps Script** — ต้องก๊อปไปวางใน editor เองถึงจะมีผลจริง (git ต่อตรงไม่ได้)

### 4.2 Apps Script — แก้ 5 จุด (วางไฟล์ใหม่ทับแล้ว)

- 🎯 **ต้นเหตุ "รายรับไม่เข้าไลน์หลายวัน"**: `SLIP_API_URL` ใน Script Properties ยังชี้บอทตัวเก่า `65gt` → `pushOwner_` ใช้ host เดียวกันจึงยิงไปที่ตายแล้ว **และ error ถูกกลืน 2 ชั้น** (`catch` ในตัวเอง + `try{}catch(e){}` ตอนเรียก) → ไม่ขึ้นเป็น failed execution เลย เหลือแต่อีเมลที่ส่งได้เสมอ
- เพิ่ม `showSlipUrl()` / `fixSlipUrl()` — ดู/เปลี่ยน host โดยคง token เดิม (ไม่โชว์ token ใน log)
- `pushOwner_` คืนข้อความ error + บอก HTTP code และวิธีแก้ (401 token / 403 env token / 400 admin id / 404 host)
- `sendDailyAlert` / `sendWeeklyAlert` → **หัวเรื่องอีเมลขึ้น `⚠️ (LINE ส่งไม่ออก)`** ถ้าไลน์พลาด (กันเงียบซ้ำ)
- เพิ่ม **`importPosByDate()`** กู้ยอดของ "วันที่ระบุ" โดยเลือกอีเมลจาก **วันที่ในหัวเรื่อง** ไม่สนชื่อไฟล์ + `debugDaily()` โชว์ "วันที่ขาด"

---

## 5. ⚠️ Gotchas (รวมของใหม่ที่เจอวันนี้)

1. **ย้ายเซิร์ฟเวอร์แล้วต้องไล่ทุกที่ที่จำ URL เก่า** — วันนี้เจอ 2 ที่ **แก้ครบแล้วทั้งคู่**: `SLIP_API_URL` ใน Apps Script · **UptimeRobot** (จี้ `65gt` ที่ตายแล้ว ขึ้น 503 มา 5 วัน 17 ชม. = **รายงาน 00:30 ไม่มีใครปลุกทั้งสัปดาห์** และไม่มีสัญญาณเตือนเลยเพราะ monitor ที่เหลือเป็นของโปรเจกต์อื่นที่ยังเขียวอยู่) เจออาการแปลกๆ ให้สงสัยข้อนี้ก่อนเลย
   → **ตอนตั้ง monitor ใหม่ ให้ติ๊ก e-mail alert ด้วยเสมอ** ไม่งั้นล่มแล้วเงียบซ้ำรอบเดิม
   → UptimeRobot ของร้านมี monitor ของ **โปรเจกต์อื่นปนอยู่** (`em-proengineering.onrender.com/healthz` — E&M ProEngineering คนละระบบ **ห้ามลบ**) ดูให้ดีก่อนลบอะไร
2. **"วันที่ขาด" ≠ "ข้อมูลหาย" เสมอไป — วันร้านหยุดก็ขึ้นว่าขาดเหมือนกัน** `debugDaily()` แยกไม่ออกเอง (ไม่มีรายงาน POS = หน้าตาเหมือนกันทั้งคู่) → เคสจริง 29 ก.ค. 2026 ร้านหยุด แต่หลุดเข้าไปอยู่ในลิสต์ "ต้องกู้" จนเกือบเสียโควตา OCR ฟรีๆ · **แก้แล้ว:** ใส่วันร้านหยุดใน `CLOSED_DAYS` (บน `debugDaily` ใน `Code_full.gs`) แล้วมันจะแยกรายงานให้ ไม่ปนกับวันที่ขาดจริง
3. **🔴 ตัวอ่านรายงาน POS เคยข้ามทั้งหมวดเงียบๆ เมื่อ 'ยอดรวมหมวด' อยู่ท้ายหน้ากระดาษ** — หัว/ท้ายกระดาษ (`Sales Report` · `From … To …` · `Category` · แถวหัวตาราง) แทรกอยู่ระหว่าง แถวยอดรวมหมวดกับชื่อหมวดถัดไป → ตัวไล่เลขย้อนหลังไปเจอปี `2569` จากบรรทัด From/To ก่อน **จึงข้ามหมวดนั้นทั้งหมวด** · เคสจริง 11/08/26: เครื่องดื่ม + เมนูต้ม + อาหารญี่ปุ่น หายไป **70,266.30** ยอดขายเหลือ 33,370.20 แทนที่จะเป็น 103,636.50 โดยไม่มีสัญญาณอะไรเลย · **แก้แล้ว** และเพิ่ม `_saleWarn_` เทียบผลรวมกับ `Real Sale`/`Total Received` ที่พิมพ์ในใบทุกครั้ง ไม่ตรงเมื่อไหร่เตือนทันที · ดูข้อความ OCR จริงได้ฟรีด้วย `DUMP_OCR = true` (ไม่กินโควตา)
4. **ชื่อไฟล์ PDF จาก POS = วันที่พิมพ์ ไม่ใช่วันเนื้อหา** — ถ้าส่งรายงานย้อนหลังหลายวันในวันเดียว ไฟล์จะลงท้ายเลขเดียวกันหมด → `importPosReports`/`backfillPos` ใช้เลขท้ายเป็นคีย์ → **ชนกัน วันหนึ่งหายไปเงียบๆ** (เคสจริง 11+12 ส.ค. ส่งซ้ำวันที่ 13) → ใช้ `importPosByDate()` แทน
5. **error ที่ถูก `catch` ไว้เฉยๆ = ระเบิดเวลา** — ทั้งบอท (`_cleanup_old_data`) และ Apps Script (`pushOwner_`) พังเงียบเป็นสัปดาห์เพราะมี try/except ครอบแล้วไม่แจ้งใคร ถ้าเขียนโค้ดเพิ่ม **ต้องมีทางให้รู้ว่าพัง**
6. **SQL ที่มี `%` จริง (LIKE) ต้อง escape ก่อนส่ง psycopg2** — `_Conn._pg_sql()` จัดการให้แล้ว ทุก query ใหม่ปลอดภัยอัตโนมัติ
7. **`ลบจ่ายล่าสุด`/`ลบบิลล่าสุด`/`ลบวันที่`** ตอนนี้คืนยอดจับคู่ให้แล้ว แต่ถ้าคู่ถูก cleanup ลบไปก่อน จะเตือน "คืนยอดไม่ครบ" → สั่ง `จัดยอดใหม่` เกลาได้
8. **Gemini key** ต้อง restrict "Gemini API only" + เปิด billing (free tier 20/min ตันตอนพีค = 429)
9. **`PAYEE_KEYWORDS`** = บัญชีรับเงินของร้าน (ไส้ย่าง/SAI YANG/944107…) · **`PAYABLE_PAYEE_KEYWORDS`** = บัญชีดวงใจ (เจ้าหนี้) — ใส่สลับกันแล้วเตือนผิดทุกใบ · OCR ชื่ออ่านเพี้ยนบ่อย ให้จับที่เลขบัญชี
10. **กลุ่มหนี้ mirror**: primary = กลุ่มส่งบิล/สลิป (reply ฟรี) · mirror = ดูสรุป (push) · ยอดค้างรวมโชว์เฉพาะ mirror · กลุ่ม mirror ส่งรูปจะไม่บันทึก
11. **`DATABASE_URL`** ต้องเป็น Supabase **Session pooler** (IPv4) — Direct connection เป็น IPv6 Render ต่อไม่ได้
12. **`/api/db_export` เคย backup ไม่ครบแบบเงียบๆ** — `_MIGRATE_TABLES` เป็นลิสต์ hardcode พอเพิ่มตาราง `recon_pending` ทีหลังแล้วลืมใส่ในลิสต์ endpoint ที่เขียนว่า "dump ทุกตาราง" ก็ข้ามมันไปโดยไม่บอกใคร (เพิ่งเจอตอนไล่นับตารางด้วยมือก่อนปิด DB เก่า 18 ส.ค.) · **แก้แล้ว:** ใส่ `recon_pending` เข้าลิสต์ + endpoint คืนฟิลด์ `missing_tables` (ต้องเป็น `[]` เสมอ — ไม่ว่างเมื่อไหร่คือ backup ขาด) + มีเทสต์ยันว่าลิสต์ครอบทุกตารางใน DB จริง
13. **env vars อยู่ที่ Render service** ไม่ใช่ repo → สร้าง service ใหม่ต้องก๊อป env ให้ครบ (กลุ่มที่มักลืม: `SLIP_API_TOKEN`, `LINE_ADMIN_USER_ID`, `REPORT_REDIRECT`)
14. **โควตา push LINE ฟรี ~300/เดือน/OA** — reply ฟรี · รายงาน/สรุปใช้ push · เช็คด้วยคำสั่ง `โควต้า`
15. **repo public** → ห้าม hardcode token/เลขบัญชี/group id (ใช้ env ทั้งหมด)

---

## 5.1 📅 ช่องว่างข้อมูลที่ "ปกติ" — ไม่ต้องไปไล่กู้ (เจ้าของยืนยัน 18 ส.ค. 2026)

ไล่มาแล้วทั้งหมด **ไม่มีข้อมูลหายจริงสักวัน** บันทึกไว้กันคนมาไล่ซ้ำ:

| ช่วง | สถานะ | เหตุผล |
|---|---|---|
| **29 + 30 ก.ค. 2026** | ร้านหยุด | ก.ค. จึงมี 29 วันจาก 31 = ครบถูกต้อง · ใส่ใน `CLOSED_DAYS` แล้ว |
| **พฤษภาคม 2569** | ไม่มีทั้งเดือนใน `saisang_data.json` | ช่วงนั้นเพิ่งเริ่มทำระบบ ยังไม่ได้เก็บข้อมูล — **ไม่ใช่ของหาย ไม่ต้องกู้** |
| **ม.ค.–มิ.ย. 2569** | มียอดรวมเดือน แต่ `daily_totals` เป็น ❌ | เดือนเก่านำเข้าก่อนมีฟีเจอร์รายวัน (ดู `juneUseExcel()`) — ยอดรวมยังถูก |
| **"วันนี้"** | `debugDaily` เคยขึ้นว่าขาด | รายงาน POS ของวัน D เข้าเมล ~00:3x ของวัน D+1 → วันนี้ยังไม่มีข้อมูลเป็นปกติ · **แก้แล้ว** ให้นับถึงเมื่อวาน (อย่าเอาวันนี้ใส่ `CLOSED_DAYS` เด็ดขาด — พอข้อมูลเข้าจริงจะถูกเมินถาวร) |

---

## 6. คู่มือแก้ปัญหาเร็ว (playbook)

| อาการ | เช็คอะไรก่อน |
|---|---|
| **สรุปรายรับไม่เข้าไลน์ (แต่อีเมลมา)** | Apps Script → `showSlipUrl()` → ถ้า host เก่า `fixSlipUrl()` → `testPushOwner()` |
| **dashboard ไม่มียอดวันไหนวันหนึ่ง** | **เช็กก่อนว่าวันนั้นร้านหยุดหรือเปล่า** (ถ้าหยุด → ใส่ใน `CLOSED_DAYS` จบ ไม่ต้องกู้) → ไม่ได้หยุด: `debugDaily()` ดู "วันที่ขาด" → ใส่ลงลิสต์ `DATES` ของ `importPosByDate()` แล้วรัน (ใส่หลายวันพร้อมกันได้) → ถ้า `❌ ไม่พบอีเมล` ให้ `debugReports()` เช็กว่ามีไฟล์ไหม |
| **รายงานสลิป 00:30 ไม่มา** | Render Logs หา `[report]` · `sent OK` = ส่งแล้ว (ดูกลุ่มปลายทาง redirect) · ไม่มีบรรทัดเลย = ไม่มีใครปลุก (UptimeRobot) · `push FAILED` = โควตา/ถูกเตะออกจากกลุ่ม |
| **ส่งซ้ำรายงานสลิปย้อนวัน** | ในกลุ่มพิมพ์ `ทดสอบรายงาน 2026-08-11` (ตอบกลับจะบอกปลายทางจริงด้วย) |
| **ยอดหนี้ดูเพี้ยน / ตัดผิดใบ** | `สรุปหนี้` → `รายการบิล` → `จัดยอดใหม่` (มี self-verify ถ้าจะเพี้ยนมันยกเลิกเอง) |
| **บอทเงียบทั้งระบบ** | เปิด `https://line-slip-bot-1-iq8e.onrender.com/health` — ต้องได้ JSON มี `slips_today` · ถ้าโหลดช้ามาก = เซิร์ฟเวอร์หลับ |
| **เช็คว่าโควตา push ใกล้เต็ม** | ในกลุ่มพิมพ์ `โควต้า` |

---

## 7. คำสั่ง/ฟังก์ชัน อ้างอิงเร็ว

### บอทในไลน์
- **ทั่วไป:** `help` · `คู่มือ` · `groupid` · `โควต้า`
- **สลิป:** ส่งรูป (ตรวจอัตโนมัติ) · `สรุป` · `สรุป 2026-08-16` · `เพิ่มสลิป 114` (วางหลายบรรทัดทีเดียวได้) · `ลบล่าสุด` / `ลบ 3` / `ลบ 19/8 3` · `ล้างวันนี้` · `ทดสอบรายงาน [วันที่]` · `ปิดรายงาน` / `เปิดรายงาน` · `ปิดสลิป` / `เปิดสลิป`
- **กู้สลิปที่หาย:** `กู้สลิป` (= เมื่อวาน) · `กู้สลิป 2026-08-22` / `กู้สลิป 22/8` · `รายการข้าม [วันที่]` (ฟรี ไม่กิน AI) — ตัวกู้ทำอัตโนมัติก่อนรายงานทุกวันอยู่แล้ว (เพดาน `REPORT_RECOVER_MAX`)
- **จอง:** พิมพ์ธรรมชาติ "จองโต๊ะคุณเอ 4คน วันนี้ 2ทุ่ม โซนA" · `สรุปจอง` (= **ทั้งร้าน** ไม่ใช่เฉพาะกลุ่มที่พิมพ์) · `ยกเลิกจอง 38`
- **หนี้:** ส่งรูปบิล/สลิป (เด้งสรุปทั้งคู่) · `บิล 3500` · `จ่าย 15980` · `จ่าย 28/7 15980` · `สรุปหนี้ [วันที่]` · `รายการบิล` · `ลบบิลล่าสุด` / `ลบจ่ายล่าสุด` · `ลบวันที่ 6/6` · `จัดยอดใหม่` · `ล้างบัญชีหนี้` · **บล็อกยอดค้าง** (วางหลายบรรทัดแบบ `31/7=9941`)

### Apps Script (เลือกจากเมนู "เลือกฟังก์ชัน" แล้วกด ▷)
| ฟังก์ชัน | ใช้เมื่อ | กิน OCR? |
|---|---|---|
| `showSlipUrl` / `fixSlipUrl` | เช็ค/แก้ลิงก์บอท | ไม่ |
| `testPushOwner` | ทดสอบส่งเข้าไลน์ | ไม่ |
| `debugDaily` | ดูว่ามีกี่วัน / ขาดวันไหน | ไม่ |
| `debugReports` | ดูไฟล์ที่มีในเมล 40 วัน | ไม่ |
| `rebuildNow` | สร้างข้อมูลเดือนใหม่ให้ dashboard | ไม่ |
| **`importPosByDate`** | **กู้ยอดวันที่ระบุ** — ใส่วันที่ในลิสต์ `DATES` ได้หลายวัน รันครั้งเดียว (วันหนึ่งพังไม่หยุดวันที่เหลือ) | ~4 ครั้ง/วัน |
| `backfillPos` | กู้หลายวันรวดเดียว (12 วัน/รอบ) | 3–4/วัน |
| `backfillZones` / `backfillVoids` / `backfillBills` / `backfillCredit` | เติมข้อมูลย่อยของวันที่เก็บแล้ว | 1/วัน |
| `forecastCashflow` / `beverageWatch` / `profitByWeekday` / `analyzeWeekday` | รายงานวิเคราะห์ | ไม่ |
| `setupDailyTrigger` | ตั้ง trigger ใหม่ (ตี1–5 + จันทร์ 9 โมง) | ไม่ |

**ชน OCR limit** (`⏳ หยุดที่ ...`) = พัก ~5 นาที แล้วรันซ้ำได้เลย ทุกตัวออกแบบให้รันซ้ำปลอดภัย

---

## 8. Git workflow (ตามที่เจ้าของตกลง)

- งานเล็ก/ด่วน (ไม่แตะ DB schema, การเงิน, logic หลายจุด) → commit + push `main` ตรง (Render auto-deploy)
- งานใหญ่/เสี่ยง (DB schema, การเงิน, แก้หลายจุด) → feature branch + PR ให้เจ้าของดู diff ก่อน merge
- ทดสอบก่อน push: `python3 -m unittest discover -s tests -q` (**335 เคส · ~0.6 วิ** · ไม่ตั้ง `DATABASE_URL` = ใช้ SQLite ชั่วคราวอัตโนมัติ ไม่แตะของจริง ไม่ต้องมีคีย์/เน็ต)
- **เขียนเทสต์จากเคสจริงเสมอ** — เทสต์ในไฟล์นี้เกือบทุกตัวมี docstring บอกว่ามาจากเหตุการณ์ไหน วันไหน อ่านแล้วรู้ทันทีว่าทำไมต้องมี
- **เทสต์ต้องยิงเข้าฟังก์ชันจริง ห้าม copy ตรรกะมาไว้ในไฟล์เทสต์** (เคยพลาดมาแล้ว — เทสต์เขียวแต่ของจริงพัง)
- **แก้ logic การเงิน (บิล/จ่าย/ยอดค้าง) ต้องรันเทสต์ก่อน push เสมอ** — บั๊กกลุ่มนี้ตาเปล่าไม่เห็น เคยพังเงียบมาแล้วหลายรอบ
- push ไป **`empro959/line-slip-bot`** เท่านั้น (`saiyangsoi-sketch` เลิกใช้แล้ว)
