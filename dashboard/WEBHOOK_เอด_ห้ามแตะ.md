# 🔀 แผนแยกระบบ: บอทร้าน (เอด) ↔ ระบบคอนเทนต์ — กันชน Webhook

> ปัญหา: LINE 1 OA มี **Webhook ได้อันเดียว** → เอาเอดไปทำ 2 ระบบพร้อมกันไม่ได้
> ผลที่เจอ: อีกห้องเปลี่ยน webhook ของเอด → **บอทสลิป/จอง/หนี้ล่มทันที**

---

## กฎเหล็ก
**1 OA = 1 Webhook = 1 ระบบ** · ห้ามใช้ OA เดียวทำ 2 ระบบที่รับ event พร้อมกัน

---

## ✅ ทางแก้ (สถาปัตยกรรมที่ถูก)

| ระบบ | OA ที่ใช้ | Webhook ชี้ไปที่ | ใครดูแล |
|---|---|---|---|
| **บอทร้าน** (สลิป/จอง/หนี้) | **เอด @lza4817e** | `https://line-slip-bot-65gt.onrender.com/callback` | ห้องนี้ (Render) |
| **คอนเทนต์** (FB/TikTok) | **OA ใหม่แยกต่างหาก** | ระบบคอนเทนต์ (line_bridge) | อีกห้อง |

### หลักการ
- **เอด = ของบอทร้านเท่านั้น** — ห้ามแตะ webhook เด็ดขาด (แตะ = ร้านล่ม)
- **คอนเทนต์ FB/TikTok ไม่จำเป็นต้องใช้ webhook ของเอดเลย** (คนละแพลตฟอร์ม โพสต์ผ่าน API ของ FB/TikTok)
- ถ้าคอนเทนต์ต้องรับคำสั่งผ่าน LINE จริงๆ → **สร้าง OA ใหม่ (ฟรี)** เช่น "ไส้ย่าง Content" แล้วชี้ webhook ไป line_bridge ของเขา

---

## 📋 ขั้นตอน (ทำตามลำดับ)

### ขั้นที่ 1 — กู้บอทร้านก่อน (ด่วน)
1. LINE OA Manager → OA **เอด** → ตั้งค่า → การตอบกลับ
2. **Webhook = เปิด** · โหมด = "แชทบอท"
3. (หรือ Developers Console → เอด → Messaging API → Webhook URL = `.../callback` + Use webhook = เปิด)
4. พิมพ์ `groupid` ในกลุ่ม → บอทตอบ = ร้านกลับมาแล้ว ✅

### ขั้นที่ 2 — ตั้งระบบคอนเทนต์แยก (อีกห้อง)
- ถ้าคอนเทนต์ต้องใช้ LINE: **สร้าง OA ใหม่** (คนละตัวกับเอด) → ชี้ webhook ไป line_bridge
- ถ้าไม่ต้องใช้ LINE: โพสต์ FB/TikTok ผ่าน API ตรง ไม่ต้องยุ่งเอด

### ขั้นที่ 3 — ตกลงกติกา (กันพลาดซ้ำ)
- เอด @lza4817e = **แตะได้ห้องเดียว (ห้องบอทร้าน)**
- ใครจะเปลี่ยน webhook/ตั้งค่าเอด = ต้องแจ้งก่อน

---

## 💬 ข้อความส่งอีกห้อง (ก๊อปวางได้เลย)

```
⚠️ อย่าแตะ webhook / ตั้งค่า OA "เอด" (@lza4817e) เด็ดขาด
เอดเป็นของบอทสลิป/จอง/หนี้ของร้าน — webhook ต้องชี้ Render เท่านั้น
(https://line-slip-bot-65gt.onrender.com/callback)
พอเปลี่ยน webhook = ระบบร้านล่มทันที (จอง/สลิป/หนี้เงียบหมด)

งานคอนเทนต์ FB/TikTok:
- โพสต์ผ่าน API ของ FB/TikTok ตรงๆ ไม่ต้องใช้ webhook เอด
- ถ้าต้องรับคำสั่งผ่าน LINE จริงๆ → สร้าง OA ใหม่แยก อย่าใช้เอด
line_bridge ให้ชี้ไป OA ใหม่ ไม่ใช่เอด
```

---

## ⚠️ ทำไมห้ามใช้ OA เดียวร่วมกัน
LINE ยิง event (ข้อความ/รูป/จอง) ไป webhook **URL เดียว** เท่านั้น:
- ชี้ Render → คอนเทนต์ไม่ได้รับ
- ชี้ line_bridge → บอทร้านไม่ได้รับ (= ที่เจอตอนนี้)
- **ไม่มีทางได้ทั้งคู่** บน OA เดียว → ต้องแยก OA

*(ทางเลือกขั้นสูง: ทำ webhook proxy กระจาย event ไป 2 ระบบ — แต่เปราะบาง/เสี่ยง signature ไม่แนะนำ เทียบกับแยก OA ที่ชัวร์กว่า)*
